# Dependencies for the read-only monitoring dashboard (dashboard.py).
# Optional - only needed if you run the dashboard. Install with:
#   pip install -r requirements-dashboard.txt
fastapi>=0.141.1
uvicorn>=0.52.1
