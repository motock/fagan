# Scratchpad — systemd usage-poller template tests

## Task (test-authoring dispatch only)
Write the test suite for the systemd matched-pair templates. Do NOT create the
implementation files (`systemd/*.template`) — a later dispatch does that.

## Done
- Created `tests/unit/test_systemd_usage_poller_templates.py` (51 tests).
- Covers: both files exist; verbatim content of both templates; `Type=oneshot`
  + no `Restart=`/`RestartSec=` on the service; timer `Unit=` names exactly
  `com.fagan.pipeline.usage-poller.service`; timer `WantedBy=timers.target`
  and service has no `WantedBy=`/`[Install]`; no hardcoded machine paths
  (`/Users/`, `/home/`, `/root/`, `/var/folders/`, `C:\Users\`) and
  `{{REPO_ROOT}}`/`{{HOME}}` kept literal; timer has no placeholder tokens.
- Helper self-tests (negative/boundary): detector flags real paths, `_directive`
  returns None for missing keys, last-wins for repeats, no prefix false-match
  (`OnUnitActiveSec=` vs `Unit=`), `_read` raises FileNotFoundError naming path.
- Verified GREEN (51 passed) against the exact spec content in a throwaway
  tmp-dir simulation; verified RED on violations (Restart=, WantedBy= on
  service, wrong timer Unit=).
- ruff check + ruff format --check both clean.

## State
- Real repo: RED as intended — 39 failed / 12 passed; failures are
  `FileNotFoundError: required systemd template missing: .../systemd/...`
  (right reason: implementation absent). 12 passing are helper self-tests.

## Next
- Commit the test file. Do not implement.
