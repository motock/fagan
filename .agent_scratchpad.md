# Scratchpad — WS-12 workspace.js test-first dispatch

## Done
- Recon: read api.js/comms.js conventions, test_app_hash.mjs structure, _app_js_loader.mjs.
  Key facts: jsdom NOT installed (hash/backend .mjs tests can't run here); loader hardcodes
  static/app.js (31-byte `export * from "./app/main.js"` shim) so it can't load workspace.js;
  escapeHtml contract in render/board.js: & < > " ' -> &amp; &lt; &gt; &quot; &#39;.
- Wrote tests/test_app_workspace.mjs (committed 389fe32): jsdom-free window stub, bootstraps
  globals through loadAppInto (try/catch — app graph wiring out of scope), cache-busted direct
  import of ../static/app/workspace.js?t=N per test.
- 14 tests, all RED with `Cannot find module .../static/app/workspace.js` (correct reason).
- ruff check . clean; no existing files touched.

## Coverage map (brief -> assertions)
- exports exist (3 fns); uses fetch; GET /api/workspaces + returns array on 200;
  500 -> []; fetch rejects -> []; malformed 200 bodies -> array;
  POST /api/workspace body {path, create} incl. create:false boundary; 2xx parsed result;
  400 detail -> error string; non-2xx w/o detail -> string error; network error no-throw;
  unparseable error body no-throw; escaping of < > & " '; valid:false marked unavailable
  AND included (lone-entry boundary); missing valid field not marked; [] -> explicit empty state.

## Next (NOT mine — later dispatch)
- Update tests/test_app_workspace.mjs to make default fetch URL-aware and add trailing newline.
- Implement static/app/workspace.js (module-level fns, named exports at bottom, fetch).
- Updated workspace.js: imported escapeHtml, added null-safe handling in renderWorkspaceList, removed local escapeHtml.
- Ran tests: all 14 passed (re-verified this dispatch: `node --test tests/test_app_workspace.mjs` -> 14/14 PASS).
- Full node run `node --test tests/*.mjs tests/*.js`: test_app_workspace PASS; 5 files FAIL.
  - test_app_hash.mjs + test_app_backend_escalated.mjs: `Cannot find package 'jsdom'` — known env gap
    (scratchpad recon: jsdom not installed here), pre-existing, unrelated to WS-12.
  - test_app_js_smoke/overview/search.js: `Cannot read properties of undefined (reading 'forEach')`
    — none of them import workspace.js (not wired into main.js; wiring is a separate story).
- Base-commit check: identical failure set at origin/agent/ws-12 AND at merge base 6d4f5c0 —
  all 5 failures pre-existing, zero regressions from WS-12. Temp worktree removed.
- ruff check . -> "All checks passed!" (clean).
- python3 -m pytest -q -> 5958 passed, 3 skipped (full suite green).
- Scope check: `git status` clean; since merge base only NEW files touched
  (static/app/workspace.js, tests/test_app_workspace.mjs, .agent_scratchpad.md);
  state.js/comms.js/main.js and all pre-existing tests unmodified.
- done: new test green (14/14), full suite green (5958 passed/3 skipped), ruff clean, scope clean.
- Next: commit scratchpad update, push branch, exit.

