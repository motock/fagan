Task: add workspace param to decompose_plan

Progress: 10/10 — venv was stale (mcp 1.28.1 vs pinned 2.1.1); reinstalled mcp==2.1.1, reverted _run_triage_sweep wrapper to plain `from .triage import run_triage_sweep`. 24/25 fixed; triage_sweep_wiring 15/15 green. Full gate run times out at 600s locally — will run in background-free chunks next.

## Step 1: Created scratchpad file.
