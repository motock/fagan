PROGRESS: 0/8

Deliverable: scripts/local_success_report.py only

Edits:
1. Docstring sentence swap
2. Insert cohort block after window print
3. overall = rolling_rate(cohort, window=0)
4. tier_rate = rolling_rate(cohort, window=0, tier=tier)

DO NOT TOUCH: rolling_rate, reason-count printing, exit codes, any test file

next: read scripts/local_success_report.py and copy the four anchor regions verbatim

Anchor A (docstring two lines):
"""
The ``--window`` flag controls the size of the rolling window; ``0``
means all stories.  The ``--plan-dir`` flag points to the directory
"""

Anchor B (window print):
    print(f"window: {args.window}")

Anchor C (overall call):
    overall = rolling_rate(all_classified, window=args.window)

Anchor D (tier call):
        tier_rate = rolling_rate(all_classified, window=args.window, tier=tier)
