# Scratchpad - server.json registry-schema regression tests (test author only)

## Done this dispatch
- tests/unit/test_server_json_length.py (new test file added to check description length). The original 141‑character description violates the MCP Registry schema ``maxLength: 100``.
- server.json reverted to original 141‑character description (bug remains).
- .agent_scratchpad.md is still tracked; the implementation dispatch will untrack it.

## Next steps (implementation dispatch)
1. Rewrite server.json with a compliant 53‑character description.
2. git rm --cached .agent_scratchpad.md.
3. Drop the two scratchpad-only commits via rebase -i.
4. Verify untracking persists after next commit.

# End of scratchpad
