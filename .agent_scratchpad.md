The goal (create `systemd/com.fagan.pipeline.advance-scheduler.service.template` verbatim), the constraint list (literal `{{REPO_ROOT}}`/`{{HOME}}` tokens, trailing newline, no `EnvironmentFile=`, no `PIPELINE_` key, no real home path, no launchd/ edits, no test edits), and "Next: read the committed test file"