# Generated benchmark workspaces, results, and scratch dirs.
_runs/
_matrixtest/
_realtest/
_dbg/
_dbg_out.txt
__pycache__/
.ruff_cache/
_realtest.out

# _repro/ scripts are kept (reusable diagnostic tooling, see
# PRODUCT_ANALYST_VALIDATION_PLAN.md); the regenerable git workspace itself
# is ignored (its logs already match the root .gitignore's *.log rule).
_repro/cell/
