Task: add workspace param to decompose_plan

Progress: 10/10 — venv was stale (mcp 1.28.1 vs pinned 2.1.1); reinstalled mcp==2.1.1, reverted _run_triage_sweep wrapper to plain `from .triage import run_triage_sweep`. 24/25 review failures green. Full gate exceeds 600s bash timeout → chunked runs. Chunk2 bisect: `test_build_detect.py` pollutes `test_check_story_status_lint_gate.py` (5 failures; `p.subprocess.run` monkeypatch not taking effect / env not passed through). NEXT: read `_run_lint_gate` in pipeline/server.py + what test_build_detect monkeypatches.

## Step 1: Created scratchpad file.
