{
  "repo_root": "/Users/jessecarroll/git/fagan",
  "epics": [
    {
      "summary": "Post-RLF hardening: reviewer send-back accounting and local-harness dead ends",
      "stories": [
        {
          "key": "PRH-1",
          "summary": "Make the scope gate's revert advice runnable on the local harness",
          "description": "The scope gate tells executors to run a git checkout the local harness blocks; its restore_file fallback re-applies the committed drift. Name only commands the harness allows.",
          "agent_instructions": "Preflight: impact run 2026-09-23 on 78b3f48 (scratch worktree, all four PRH stories' real edits applied together) - 12954 passed / 12 skipped / 0 failed; `ruff check .` clean under ruff 0.16.7 (CI's pinned version). The fixture was verified against a mutant that restores the old remedy sentence: 7 of its 12 tests then fail on assertions, and all 12 pass with the real edits.\n\nTARGET IMPLEMENTER: on-device (~20B class). Pin: backend \"ollama\", model \"gpt-oss-20b-high:latest\". Two production files (pipeline/scope_gate.py, pipeline/review_orchestrator.py), one new module constant, no new functions, no documentation change.\n\n[no-new-tests] The acceptance fixture named below is the complete, already-verified suite for this story. It is already in your worktree at the stated path. Do NOT write a second test suite, and do NOT modify, rename or delete that file. It is digest-pinned: any edit to it, even whitespace-only, marks it tampered and blocks the story. Never run `ruff format`, black or `ruff check --fix` over it or over any file. Use plain `.venv/bin/python -m ruff check .` to check hygiene. Commit the fixture together with your change (`git add` it) so it lands in the PR.\nFixture: tests/unit/test_scope_gate_remedy_runnable.py (12 tests).\n\nWHY (the defect)\nWhen a story's branch changes files outside its declared `files` scope, the scope gate sends it back with feedback that tells the executor to run `git checkout <base> -- <path>`. A local-agent executor cannot follow that: its bash tool refuses the command (`destructive_git_op` in pipeline/local_agent_common.py matches `git checkout ... -- <path>`), the refusal then points it at `restore_file`, and `restore_file` restores from the LAST COMMIT, which already contains the out-of-scope change, so it re-applies the very edit being removed. Nothing errors. Story RLF-1 burned three dispatches in this loop. The remedy must name only commands the harness allows, and must warn off `restore_file`.\n\nWHAT TO BUILD (3 anchored edits, 2 files, 0 new functions). Apply each with `str_replace`, using the OLD text exactly as shown.\n\n----- EDIT 1 - pipeline/scope_gate.py (insert the constant): OLD (must match exactly once) -----\n# Upper bound for each git invocation; the gate fails open on timeout.\n----- NEW -----\n# The remedy appended to the gate's REQUEST_CHANGES feedback. Its reader may be\n# a local agent whose bash tool refuses destructive git commands and whose\n# restore_file tool restores from the LAST COMMIT, which already contains the\n# out-of-scope change. So the remedy must be runnable there: it names only\n# commands that harness allows and never spells out a blocked one.\nSCOPE_GATE_REMEDY = (\n    \"Revert each listed change so the branch matches the base again. \"\n    \"For a file that exists on the base branch, restore its exact base \"\n    \"content with `git show origin/<default-branch>:<path> > <path>` (the \"\n    \"default branch as fetched from origin, e.g. origin/master; never a \"\n    \"possibly stale local branch). \"\n    \"For a file this branch added, run `git rm <path>`. \"\n    \"Do not use restore_file for this: it restores from the last commit, \"\n    \"which already contains the out-of-scope change. \"\n    \"Then commit the revert in the same step, so the last commit stops \"\n    \"carrying it. \"\n    \"If the brief requires a listed change, stop and report the conflict.\"\n)\n\n# Upper bound for each git invocation; the gate fails open on timeout.\n----- end -----\n\n----- EDIT 2 - pipeline/review_orchestrator.py (import): OLD (must match exactly once) -----\nfrom .scope_gate import SCOPE_GATE_HEADER, check_branch_scope\n----- NEW -----\nfrom .scope_gate import SCOPE_GATE_HEADER, SCOPE_GATE_REMEDY, check_branch_scope\n----- end -----\n\n----- EDIT 3 - pipeline/review_orchestrator.py (use it; this is one line inside `review_story`): OLD (must match exactly once) -----\n            + \"\\nRevert each listed change (git checkout <base> -- <path> for an edit, git rm for an added file) unless the brief requires it; if it does, stop and report the conflict.\\n\"\n----- NEW -----\n            + \"\\n\" + SCOPE_GATE_REMEDY + \"\\n\"\n----- end -----\n\nWhat the fixture grades (implement exactly this): the remedy is not refused by `destructive_git_op`; the guard STILL refuses `git checkout origin/master -- app/backend.py` (do not touch the guard); the remedy names `git show origin/<default-branch>:<path> > <path>`, `git rm <path>`, the words `commit the revert in the same step`, and `restore_file` with `last commit`; it never spells `git checkout`, `git restore`, `git reset --hard` or `git clean`; and the real `review_story` puts exactly that text into `review_feedback` for an out-of-scope branch and never for an in-scope one.\n\nSURVIVORS (do not change)\n- `SCOPE_GATE_HEADER`, the `- Blocking: <path>` lines, the trailing `VERDICT: REQUEST_CHANGES` line, the `_notify_user(... event=\"scope_gate_failed\" ...)` call and everything else in `review_story`.\n- `pipeline/local_agent_common.py` and `scripts/local_agent_tools.py`: the guard and `restore_file` are correct; only the advice was wrong.\n- tests/unit/test_review_scope_gate.py and tests/unit/test_scope_gate.py pass UNMODIFIED.\n\nSUCCESS CRITERIA\n- `.venv/bin/python -m pytest -q tests/unit/test_scope_gate_remedy_runnable.py tests/unit/test_review_scope_gate.py tests/unit/test_scope_gate.py` is green.\n- Full suite green and `.venv/bin/python -m ruff check .` clean. Report the counts.\n- `git diff origin/master --stat` touches exactly: pipeline/scope_gate.py, pipeline/review_orchestrator.py, tests/unit/test_scope_gate_remedy_runnable.py.\n- Suggested commit: `fix(scope-gate): make the revert advice runnable on the local harness`.\n\nHARD CONSTRAINTS (read before touching anything)\n- Do NOT call `restore_file`, `git checkout ... -- <path>`, `git restore`, `git reset --hard` or `git clean` on anything. The harness blocks most of them, and `restore_file` restores from the LAST COMMIT, which is not the base branch. To undo one of your own edits, put the original text back with `str_replace`.\n- Compare against `origin/master`, NEVER the bare `master` ref. The local `master` branch is deliberately stale, so `git diff master` lists files from stories merged since it last moved. Those are not yours: never revert, restore or edit a file that is not on your `files` list.\n- READ-TOOL CAP, NOT FILE CORRUPTION: a `view_file` result that ends in a truncation note means the read tool stopped, the file is intact. Re-read a narrower line range. Never conclude a file is damaged and never rewrite one to \"repair\" it.\n- Use `.venv/bin/python -m pytest` and `.venv/bin/python -m ruff`, never a bare `pytest` or `ruff`.\n- Run the full suite AT MOST TWICE; re-running a green suite burns your steps. When you have enough information, act.\n- `.agent_scratchpad.md` must NEVER be committed. If it shows up tracked, run `git rm --cached .agent_scratchpad.md`.\n- Do not add features, refactor adjacent code, or reformat lines you did not have to change.\n- Commit with Conventional Commits, imperative subject <=72 chars, and a body that explains WHY.\n",
          "acceptance": [
            {
              "path": "tests/unit/test_scope_gate_remedy_runnable.py",
              "source": "\"\"\"The scope gate's remedy must be runnable by the executor that receives it.\n\nThe gate sends an out-of-scope branch back with REQUEST_CHANGES feedback that\ntells the executor how to revert. A local-agent executor cannot follow advice\nthat spells a command its bash tool refuses (``git checkout <base> -- <path>``\nis matched by ``destructive_git_op``), and its ``restore_file`` fallback\nrestores from the LAST COMMIT, which already carries the out-of-scope change.\nSo the remedy must name only commands the harness allows.\n\nThese tests drive the REAL ``pipeline.server.review_story`` entry point against\na real git repo, so the feedback grade covers the wiring and not just the\nconstant. Fixture shape is cribbed from ``tests/unit/test_review_scope_gate.py``.\n\"\"\"\n\nimport json\nimport subprocess\n\nimport pytest\n\n# Import pipeline.server FIRST: pipeline.review_orchestrator -> server ->\n# review_orchestrator is a circular import, so importing the orchestrator\n# standalone raises ImportError. Importing the server module first breaks the\n# cycle (same idiom as tests/unit/test_review_scope_gate.py).\nimport pipeline.server as p\nfrom pipeline import concurrency as pcon\nfrom pipeline import persistence as ppers\nfrom pipeline.local_agent_common import destructive_git_op\nfrom pipeline.scope_gate import SCOPE_GATE_REMEDY\n\n\n@pytest.fixture\ndef plan_dir(tmp_path, monkeypatch):\n    d = tmp_path / \"plans\"\n    d.mkdir()\n    monkeypatch.setattr(p, \"PLAN_DIR\", d)\n    monkeypatch.setattr(ppers, \"PLAN_DIR\", d)\n    monkeypatch.setattr(pcon, \"PLAN_DIR\", d)\n    return d\n\n\ndef _git(repo, *args):\n    subprocess.run([\"git\", *args], cwd=repo, check=True, capture_output=True, text=True)\n\n\ndef _init_repo(tmp_path):\n    \"\"\"A repo on branch ``main`` with a base commit, then branch ``feature``.\"\"\"\n    repo = tmp_path / \"repo\"\n    repo.mkdir()\n    _git(repo, \"init\")\n    _git(repo, \"config\", \"user.email\", \"t@example.com\")\n    _git(repo, \"config\", \"user.name\", \"T\")\n    (repo / \"pipeline\").mkdir()\n    (repo / \"pipeline\" / \"foo.py\").write_text(\"x = 1\\n\")\n    _git(repo, \"add\", \"-A\")\n    _git(repo, \"commit\", \"-m\", \"base\")\n    _git(repo, \"branch\", \"-M\", \"main\")\n    _git(repo, \"checkout\", \"-b\", \"feature\")\n    return repo\n\n\ndef _send_back_or_review(plan_dir, monkeypatch, repo):\n    \"\"\"Run review_story on ``repo`` (story scope: pipeline/foo.py only) with the\n    LLM reviewer stubbed; return the review_feedback stored in the manifest.\"\"\"\n    monkeypatch.setattr(p, \"_auto_escalation_enabled\", lambda: False)\n    monkeypatch.setattr(p, \"_notify_user\", lambda *a, **k: None)\n    monkeypatch.setattr(\n        p, \"_run_reviewer\", lambda *a, **k: \"Add a test.\\nVERDICT: REQUEST_CHANGES\"\n    )\n    story = {\n        \"summary\": \"Add thing\",\n        \"status\": \"tests_passed\",\n        \"worktree\": str(repo),\n        \"risk\": \"low\",\n        \"files\": [\"pipeline/foo.py\"],\n    }\n    manifest = {\"epics\": {}, \"stories\": {\"S1\": story}}\n    (plan_dir / \"pe.manifest.json\").write_text(json.dumps(manifest))\n    p.review_story(\"pe\", \"S1\")\n    saved = json.loads((plan_dir / \"pe.manifest.json\").read_text())\n    return saved[\"stories\"][\"S1\"][\"review_feedback\"]\n\n\ndef test_remedy_is_not_refused_by_the_local_agent_guard():\n    assert destructive_git_op(SCOPE_GATE_REMEDY) is None\n\n\ndef test_guard_still_refuses_the_old_remedy_command():\n    # Guards the fix's direction: the remedy changed, the guard did not relax.\n    old = \"git checkout origin/master -- app/backend.py\"\n    assert destructive_git_op(old) == \"git checkout -- <paths>\"\n\n\n@pytest.mark.parametrize(\n    \"needle\",\n    [\n        \"git show origin/<default-branch>:<path> > <path>\",\n        \"git rm <path>\",\n        \"commit the revert in the same step\",\n    ],\n)\ndef test_remedy_prescribes_the_runnable_revert_and_the_commit(needle):\n    assert needle in SCOPE_GATE_REMEDY\n\n\ndef test_remedy_warns_that_restore_file_reapplies_the_committed_change():\n    assert \"restore_file\" in SCOPE_GATE_REMEDY\n    assert \"last commit\" in SCOPE_GATE_REMEDY\n\n\n@pytest.mark.parametrize(\n    \"blocked\", [\"git checkout\", \"git restore\", \"git reset --hard\", \"git clean\"]\n)\ndef test_remedy_never_spells_a_blocked_git_command(blocked):\n    assert blocked not in SCOPE_GATE_REMEDY\n\n\ndef test_sent_back_feedback_carries_a_runnable_remedy(plan_dir, monkeypatch):\n    repo = _init_repo(plan_dir.parent)\n    (repo / \"pipeline\" / \"foo.py\").write_text(\"x = 2\\n\")\n    (repo / \"stray.py\").write_text(\"y = 1\\n\")\n    _git(repo, \"add\", \"-A\")\n    _git(repo, \"commit\", \"-m\", \"out of scope\")\n\n    feedback = _send_back_or_review(plan_dir, monkeypatch, repo)\n\n    assert \"SCOPE GATE\" in feedback\n    assert \"stray.py\" in feedback\n    assert SCOPE_GATE_REMEDY in feedback\n    assert \"git checkout <base>\" not in feedback\n    assert destructive_git_op(feedback) is None\n    assert feedback.rstrip().endswith(\"VERDICT: REQUEST_CHANGES\")\n\n\ndef test_in_scope_branch_feedback_does_not_carry_the_remedy(plan_dir, monkeypatch):\n    repo = _init_repo(plan_dir.parent)\n    (repo / \"pipeline\" / \"foo.py\").write_text(\"x = 2\\n\")\n    _git(repo, \"add\", \"-A\")\n    _git(repo, \"commit\", \"-m\", \"in scope\")\n\n    feedback = _send_back_or_review(plan_dir, monkeypatch, repo)\n\n    assert SCOPE_GATE_REMEDY not in feedback\n    assert \"SCOPE GATE\" not in feedback\n"
            }
          ],
          "persona": "software-engineer",
          "risk": "low",
          "backend": "ollama",
          "model": "gpt-oss-20b-high:latest",
          "files": [
            "pipeline/scope_gate.py",
            "pipeline/review_orchestrator.py"
          ]
        },
        {
          "key": "PRH-2",
          "summary": "Cut a capped view_file result on a line boundary and say the file is intact",
          "description": "view_file truncates at a raw character offset and its marker claims the whole requested range was shown; a weak executor read the ragged tail as corruption. Fix both verbatim-twin harness copies.",
          "agent_instructions": "Preflight: impact run 2026-09-23 on 78b3f48 (scratch worktree, all four PRH stories' real edits applied together) - 12954 passed / 12 skipped / 0 failed; `ruff check .` clean under ruff 0.16.7 (CI's pinned version). The fixture was verified RED on the unedited harness (18 of its 24 tests fail on assertions, 0 import errors; the 6 that pass are the regression guards) and green with the real edits; the 227 pre-existing view_file tests pass unmodified.\n\nTARGET IMPLEMENTER: on-device (~20B class). Pin: backend \"ollama\", model \"gpt-oss-20b-high:latest\". Two production files (scripts/local_agent_tools.py and its VERBATIM TWIN scripts/local_agent_oracle_tools.py, ~380 and ~350 lines), one new helper function per file, no documentation change.\n\n[no-new-tests] The acceptance fixture named below is the complete, already-verified suite for this story. It is already in your worktree at the stated path. Do NOT write a second test suite, and do NOT modify, rename or delete that file. It is digest-pinned: any edit to it, even whitespace-only, marks it tampered and blocks the story. Never run `ruff format`, black or `ruff check --fix` over it or over any file. Use plain `.venv/bin/python -m ruff check .` to check hygiene. Commit the fixture together with your change (`git add` it) so it lands in the PR.\nFixture: tests/unit/test_view_file_truncation_marker.py (12 tests x both harnesses = 24).\n\nWHY (the defect)\n`view_file` caps its output at 3000 characters by slicing at a raw character offset, which can land in the middle of a line (live: mid-import). Its marker then claims `showing <path> lines <start>-<end>` where <end> is the REQUESTED end, not the last line shown. A weak executor read the ragged tail as file corruption and \"repaired\" the intact file (story RLF-1). The cut must fall on a line boundary, and the marker must name the last line actually shown, say the file is intact, and say where to resume.\n\nWHAT TO BUILD. The SAME three edits go into BOTH files (the two files are deliberate verbatim twins; the fixture grades both). Apply each with `str_replace`, using the OLD text exactly as shown; each OLD text occurs exactly once per file. Do file 1 completely, then file 2.\n\n----- EDIT 1 (in each file): OLD (must match exactly once) -----\nRESTORE_FILE_MAX_PER_PATH = 2\n\n\ndef run_tool_impl(origin, fn, args) -> str:\n----- NEW -----\nRESTORE_FILE_MAX_PER_PATH = 2\n\n\ndef _cut_view_at_line_boundary(formatted: str, cap: int = 3000) -> tuple[str, int]:\n    \"\"\"Longest prefix of ``formatted`` that ends on a line boundary within ``cap``\n    characters, plus how many whole lines it holds. When even the first line is\n    longer than ``cap`` there is no boundary to cut at: hard-cut at ``cap`` and\n    report 0 lines, so the caller can say the cut fell inside a line.\"\"\"\n    boundary = formatted.rfind(\"\\n\", 0, cap)\n    if boundary == -1:\n        return formatted[:cap], 0\n    kept = formatted[: boundary + 1]\n    return kept, kept.count(\"\\n\")\n\n\ndef run_tool_impl(origin, fn, args) -> str:\n----- end -----\n\n----- EDIT 2 (in each file): OLD (must match exactly once) -----\n            return (\n                formatted[:3000]\n                + f\"\\n... [truncated; showing {args['path']} lines {start}-{end} — \"\n                  f\"call view_file again with a narrower line_start/line_end range to see more]\"\n            )\n----- NEW -----\n            kept, shown = _cut_view_at_line_boundary(formatted)\n            if shown == 0:\n                return (\n                    kept\n                    + f\"\\n... [truncated inside line {start}, which is longer than the view cap; \"\n                      f\"the file itself is intact - call view_file again with a narrower line_start/line_end range]\"\n                )\n            return (\n                kept\n                + f\"... [truncated after line {start + shown - 1} of the requested {start}-{end}; \"\n                  f\"the file itself is intact - call view_file again with line_start={start + shown} to continue]\"\n            )\n----- end -----\n\n----- EDIT 3 (in each file): OLD (must match exactly once) -----\n        return (\n            formatted[:3000]\n            + f\"\\n... [truncated; {args['path']} has {len(lines)} lines total — \"\n              f\"call view_file again with line_start/line_end to see more]\"\n        )\n----- NEW -----\n        kept, shown = _cut_view_at_line_boundary(formatted)\n        if shown == 0:\n            return (\n                kept\n                + f\"\\n... [truncated inside line 1, which is longer than the view cap; the file itself is intact \"\n                  f\"({len(lines)} lines) - call view_file again with line_start/line_end to narrow the view]\"\n            )\n        return (\n            kept\n            + f\"... [truncated after line {shown} of {len(lines)}; the file itself is intact - \"\n              f\"call view_file again with line_start={shown + 1} (and line_end) to continue]\"\n        )\n----- end -----\n\nWhat the fixture grades (implement exactly this): a capped view (ranged or whole-file) ends on a whole line; the marker says `truncated after line N` where N is the last line actually shown, contains `the file itself is intact`, and gives `line_start=<N+1>`; the ranged marker no longer says `showing` or claims the requested range; resuming at N+1 skips nothing; a view of exactly 3000 characters is not truncated, one line past it keeps every whole line that fits; a first (or ranged) line longer than the cap is hard-cut and the marker says `truncated inside line <n>` and `the file itself is intact`; a small file's view is byte-identical to before; total output stays within cap + 400 characters.\n\nSURVIVORS (do not change)\n- Every other branch of `run_tool_impl` (create_file, str_replace, replace_lines, restore_file, bash, ...) and `safe_run_tool_impl`.\n- The 3000-character cap itself and the `if len(formatted) <= 3000: return formatted` lines.\n- The existing view_file tests pass UNMODIFIED: tests/unit/test_local_agent_view_file_cap.py, test_local_agent_syntax_guards.py, test_local_agent_oracle_syntax_guards.py, test_local_agent_oracle_view_and_tamper.py, test_local_agent_park_and_view.py. They require the words `truncated`, `line_start`, `line_end` and the total line count in the marker; the edits above keep all of them.\n\nSUCCESS CRITERIA\n- `.venv/bin/python -m pytest -q tests/unit/test_view_file_truncation_marker.py tests/unit/test_local_agent_view_file_cap.py tests/unit/test_local_agent_syntax_guards.py tests/unit/test_local_agent_oracle_syntax_guards.py tests/unit/test_local_agent_oracle_view_and_tamper.py tests/unit/test_local_agent_park_and_view.py` is green (251 tests).\n- Full suite green and `.venv/bin/python -m ruff check .` clean. Report the counts.\n- `git diff origin/master --stat` touches exactly: scripts/local_agent_tools.py, scripts/local_agent_oracle_tools.py, tests/unit/test_view_file_truncation_marker.py.\n- Suggested commit: `fix(local-agent): cut a capped view_file on a line boundary and say the file is intact`.\n\nHARD CONSTRAINTS (read before touching anything)\n- Do NOT call `restore_file`, `git checkout ... -- <path>`, `git restore`, `git reset --hard` or `git clean` on anything. The harness blocks most of them, and `restore_file` restores from the LAST COMMIT, which is not the base branch. To undo one of your own edits, put the original text back with `str_replace`.\n- Compare against `origin/master`, NEVER the bare `master` ref. The local `master` branch is deliberately stale, so `git diff master` lists files from stories merged since it last moved. Those are not yours: never revert, restore or edit a file that is not on your `files` list.\n- READ-TOOL CAP, NOT FILE CORRUPTION: a `view_file` result that ends in a truncation note means the read tool stopped, the file is intact. Re-read a narrower line range. Never conclude a file is damaged and never rewrite one to \"repair\" it.\n- Use `.venv/bin/python -m pytest` and `.venv/bin/python -m ruff`, never a bare `pytest` or `ruff`.\n- Run the full suite AT MOST TWICE; re-running a green suite burns your steps. When you have enough information, act.\n- `.agent_scratchpad.md` must NEVER be committed. If it shows up tracked, run `git rm --cached .agent_scratchpad.md`.\n- Do not add features, refactor adjacent code, or reformat lines you did not have to change.\n- Commit with Conventional Commits, imperative subject <=72 chars, and a body that explains WHY.\n",
          "acceptance": [
            {
              "path": "tests/unit/test_view_file_truncation_marker.py",
              "source": "\"\"\"view_file's truncation marker must not make an intact file look corrupt.\n\nA capped view used to cut at a raw character offset, which could land in the\nmiddle of a line (live: mid-import), and its marker claimed to show the whole\nREQUESTED range. A weak executor read the ragged tail as file corruption and\n\"repaired\" it. The cut must fall on a line boundary, and the marker must state\nthe last line actually shown, that the file is intact, and where to resume.\n\nBoth scripts/local_agent_tools.py and its verbatim twin\nscripts/local_agent_oracle_tools.py implement the cap, so every test runs\nagainst both.\n\"\"\"\n\nimport re\n\nimport pytest\n\nfrom tests.unit._local_agent_oracle_test_helpers import lao\nfrom tests.unit._local_agent_test_helpers import la\n\nCAP = 3000\n_ROW = re.compile(r\"^\\s*(\\d+)\\| (.*)$\")\n_CLAIM = re.compile(r\"truncated after line (\\d+)\")\n\n\n@pytest.fixture(params=[la, lao], ids=[\"local_agent\", \"local_agent_oracle\"])\ndef harness(request, tmp_path, monkeypatch):\n    monkeypatch.setattr(request.param, \"CWD\", tmp_path)\n    return request.param\n\n\ndef _write_lines(tmp_path, count, width=0):\n    lines = [f\"x{k} = {k}\".ljust(width) + \"\\n\" for k in range(1, count + 1)]\n    (tmp_path / \"big.py\").write_text(\"\".join(lines))\n\n\ndef _split(out):\n    body, _, marker = out.partition(\"... [truncated\")\n    return body, \"... [truncated\" + marker\n\n\ndef _rows(body):\n    matches = [_ROW.match(line) for line in body.splitlines()]\n    assert all(matches)\n    return [(int(m.group(1)), m.group(2)) for m in matches]\n\n\ndef _claimed_last_line(marker):\n    return int(_CLAIM.search(marker).group(1))\n\n\ndef test_ranged_cut_ends_on_a_whole_line(harness, tmp_path):\n    _write_lines(tmp_path, 3000)\n    out = harness.run_tool(\"view_file\", {\"path\": \"big.py\", \"line_start\": 1, \"line_end\": 3000})\n    body, _ = _split(out)\n    number, content = _rows(body)[-1]\n    assert body.endswith(\"\\n\")\n    assert content == f\"x{number} = {number}\"\n\n\ndef test_ranged_marker_names_the_last_line_actually_shown(harness, tmp_path):\n    _write_lines(tmp_path, 3000)\n    out = harness.run_tool(\"view_file\", {\"path\": \"big.py\", \"line_start\": 1, \"line_end\": 3000})\n    body, marker = _split(out)\n    assert _claimed_last_line(marker) == _rows(body)[-1][0]\n\n\ndef test_ranged_marker_says_the_file_is_intact_and_where_to_resume(harness, tmp_path):\n    _write_lines(tmp_path, 3000)\n    out = harness.run_tool(\"view_file\", {\"path\": \"big.py\", \"line_start\": 1, \"line_end\": 3000})\n    body, marker = _split(out)\n    last = _rows(body)[-1][0]\n    assert \"the file itself is intact\" in marker\n    assert f\"line_start={last + 1}\" in marker\n\n\ndef test_ranged_marker_does_not_claim_the_whole_requested_range_was_shown(harness, tmp_path):\n    _write_lines(tmp_path, 3000)\n    out = harness.run_tool(\"view_file\", {\"path\": \"big.py\", \"line_start\": 1, \"line_end\": 3000})\n    _, marker = _split(out)\n    assert \"showing\" not in marker\n    assert \" lines 1-3000\" not in marker\n\n\ndef test_resuming_at_the_reported_line_skips_nothing(harness, tmp_path):\n    _write_lines(tmp_path, 3000)\n    first = harness.run_tool(\"view_file\", {\"path\": \"big.py\", \"line_start\": 1, \"line_end\": 3000})\n    last = _rows(_split(first)[0])[-1][0]\n    second = harness.run_tool(\n        \"view_file\", {\"path\": \"big.py\", \"line_start\": last + 1, \"line_end\": last + 3}\n    )\n    assert _rows(second)[0][0] == last + 1\n\n\ndef test_ranged_view_stays_within_the_cap_plus_the_marker(harness, tmp_path):\n    _write_lines(tmp_path, 3000)\n    out = harness.run_tool(\"view_file\", {\"path\": \"big.py\", \"line_start\": 1, \"line_end\": 3000})\n    assert len(out) <= CAP + 400\n\n\ndef test_whole_file_cut_ends_on_a_whole_line_and_names_it(harness, tmp_path):\n    _write_lines(tmp_path, 3000)\n    out = harness.run_tool(\"view_file\", {\"path\": \"big.py\"})\n    body, marker = _split(out)\n    number, content = _rows(body)[-1]\n    assert out.startswith(\"   1| x1 = 1\")\n    assert content == f\"x{number} = {number}\"\n    assert _claimed_last_line(marker) == number\n    assert \"of 3000\" in marker\n    assert \"the file itself is intact\" in marker\n    assert f\"line_start={number + 1}\" in marker\n\n\ndef test_view_exactly_at_the_cap_is_not_truncated(harness, tmp_path):\n    # 50 lines x 60 formatted chars = exactly 3000.\n    _write_lines(tmp_path, 50, width=53)\n    out = harness.run_tool(\"view_file\", {\"path\": \"big.py\"})\n    assert len(out) == CAP\n    assert \"truncated\" not in out\n\n\ndef test_view_one_line_past_the_cap_keeps_every_whole_line_that_fits(harness, tmp_path):\n    _write_lines(tmp_path, 51, width=53)\n    out = harness.run_tool(\"view_file\", {\"path\": \"big.py\"})\n    body, marker = _split(out)\n    assert len(_rows(body)) == 50\n    assert _claimed_last_line(marker) == 50\n    assert \"of 51\" in marker\n\n\ndef test_small_file_view_is_unchanged(harness, tmp_path):\n    (tmp_path / \"small.py\").write_text(\"x = 1\\ny = 2\\n\")\n    out = harness.run_tool(\"view_file\", {\"path\": \"small.py\"})\n    assert out == \"   1| x = 1\\n   2| y = 2\\n\"\n\n\ndef test_a_first_line_longer_than_the_cap_is_hard_cut_and_says_so(harness, tmp_path):\n    (tmp_path / \"big.py\").write_text(\"a\" * 5000 + \"\\nsecond\\n\")\n    out = harness.run_tool(\"view_file\", {\"path\": \"big.py\"})\n    assert out.startswith(\"   1| \" + \"a\" * 100)\n    assert \"truncated inside line 1\" in out\n    assert \"the file itself is intact\" in out\n    assert len(out) <= CAP + 400\n\n\ndef test_a_ranged_line_longer_than_the_cap_names_that_line(harness, tmp_path):\n    (tmp_path / \"big.py\").write_text(\"short\\n\" + \"b\" * 5000 + \"\\nlast\\n\")\n    out = harness.run_tool(\"view_file\", {\"path\": \"big.py\", \"line_start\": 2, \"line_end\": 3})\n    assert \"truncated inside line 2\" in out\n    assert \"the file itself is intact\" in out\n"
            }
          ],
          "persona": "software-engineer",
          "risk": "low",
          "backend": "ollama",
          "model": "gpt-oss-20b-high:latest",
          "files": [
            "scripts/local_agent_tools.py",
            "scripts/local_agent_oracle_tools.py"
          ]
        },
        {
          "key": "PRH-3",
          "summary": "Count a reviewer send-back as a rework cycle and a dirty first pass",
          "description": "review_story emits no metric record when it sends a story back, so review rework is invisible and a thrice-bounced story counts as first-pass clean. Emit one review_changes_requested event and count it as rework and as a first-pass disqualifier.",
          "agent_instructions": "Preflight: impact run 2026-09-23 on 78b3f48 (scratch worktree, all four PRH stories' real edits applied together) - 12954 passed / 12 skipped / 0 failed; `ruff check .` clean under ruff 0.16.7 (CI's pinned version). The run first surfaced 5 pre-existing tests broken by this change; they are pre-authorized below with exact text. The fixture was verified RED on the unedited code (9 of its 14 tests fail on assertions, 0 import errors; the 5 that pass are controls) and green with the real edits.\n\nTARGET IMPLEMENTER: cloud open-source. Pin: backend \"ollama\", model \"glm-5.3-flash:cloud\". This story edits pipeline/review_orchestrator.py (~890 lines, an anchored insertion inside the long `review_story` function) and REFERENCE.md (~1590 lines), both over what the on-device tier should touch.\n\nDEPENDS ON PRH-1 (\"Make the scope gate's revert advice runnable on the local harness\"): both edit pipeline/review_orchestrator.py, in different places (PRH-1 near the `SCOPE_GATE_REMEDY` line ~195, this story near `attempts = story.get(\"rework_attempts\", 0) + 1` ~line 829). Do not start until PRH-1 has merged and you have rebased; re-read the region before editing.\n\n[no-new-tests] The acceptance fixture named below is the complete, already-verified suite for this story. It is already in your worktree at the stated path. Do NOT write a second test suite, and do NOT modify, rename or delete that file. It is digest-pinned: any edit to it, even whitespace-only, marks it tampered and blocks the story. Never run `ruff format`, black or `ruff check --fix` over it or over any file. Use plain `.venv/bin/python -m ruff check .` to check hygiene. Commit the fixture together with your change (`git add` it) so it lands in the PR.\nFixture: tests/unit/test_review_changes_requested_metrics.py (14 tests).\n\nWHY (the defect)\nWhen a reviewer (or the scope gate) sends a story back with REQUEST_CHANGES, `review_story` spends a rework attempt and redispatches, but emits NO metric record. So the plan report shows zero rework for it, and a story bounced three times and then merged is reported as first-pass clean. The only related record is `scope_gate_failed`, which covers just one of the two bounce routes.\n\nDECISIONS ALREADY MADE BY THE OWNER (do not revisit)\n- One event, `review_changes_requested`, emitted on EVERY bounce, scope gate included (it flows through the same handler).\n- It counts as one rework cycle AND disqualifies first-pass clean (a reviewer bounce is a dirty first pass, unlike environment-caused rework). It is therefore deliberately in BOTH metric sets.\n- `scope_gate_failed` stays informational: do NOT add it to either set (that would double-count one rejection, and it keeps historical sidecars unchanged).\n- Do NOT add the new event to `DEFAULT_OUTBOX_EVENTS` in pipeline/notification_outbox.py: only the listed events are ever emailed, and a review bounce must never email the owner.\n\nWHAT TO BUILD (4 production edits, 3 files, 0 new functions). Apply each with `str_replace`, using the OLD text exactly as shown.\n\n----- EDIT 1 - pipeline/review_orchestrator.py: OLD (must match exactly once) -----\n        attempts = story.get(\"rework_attempts\", 0) + 1\n        story[\"rework_attempts\"] = attempts\n----- NEW -----\n        attempts = story.get(\"rework_attempts\", 0) + 1\n        story[\"rework_attempts\"] = attempts\n        _notify_user(\n            plan_name,\n            f\"{story_key} sent back by review: changes requested (rework cycle {attempts}).\",\n            event=\"review_changes_requested\",\n            story_key=story_key,\n            **_rework_kwargs,\n        )\n----- end -----\n\n----- EDIT 2 - pipeline/story_metrics.py: OLD (must match exactly once) -----\n        \"dispatch_watchdog_timeout\",\n    }\n)\n----- NEW -----\n        \"dispatch_watchdog_timeout\",\n        \"review_changes_requested\",\n    }\n)\n----- end -----\n\n----- EDIT 3 - pipeline/story_metrics.py: OLD (must match exactly once) -----\n# cannot see), so a rework counts toward total_rework_cycles and the story's cost, but never against first-pass clean.\n_FIRST_PASS_DISQUALIFYING_EVENTS = frozenset(\n    {\"escalated\", \"model_fallback\", \"story_parked\", \"brief_patched\"}\n)\n----- NEW -----\n# cannot see), so a rework counts toward total_rework_cycles and the story's cost, but never against first-pass clean.\n# The one exception is review_changes_requested: a reviewer sending a story back is a dirty first pass by\n# definition, not an environment fluke, so it is deliberately in BOTH sets.\n_FIRST_PASS_DISQUALIFYING_EVENTS = frozenset(\n    {\"escalated\", \"model_fallback\", \"story_parked\", \"brief_patched\", \"review_changes_requested\"}\n)\n----- end -----\n\n----- EDIT 4 - REFERENCE.md: OLD (must match exactly once) -----\n`REWORK_MAX_ATTEMPTS` check) applies exactly as for an LLM REQUEST_CHANGES.\nThe gate fails open----- NEW -----\n`REWORK_MAX_ATTEMPTS` check) applies exactly as for an LLM REQUEST_CHANGES.\nThat handling notifies once with the `review_changes_requested` event, for the\ngate and the LLM reviewer alike; in the story metrics it counts as one rework\ncycle and disqualifies the story from first-pass clean. `scope_gate_failed` is\ninformational only and is not counted, so a gate rejection is not counted twice.\nThe gate fails open----- end -----\n\nNotes: `_notify_user` and `_rework_kwargs` already exist in scope at the emit site (they are used a few lines below); import nothing. Keep the message wording as written and never end it with \"failed\". The emit must sit immediately after `story[\"rework_attempts\"] = attempts`, so it fires only when a rework attempt is actually spent: the early-return auto-amend path above it, and inconclusive/rate-limited reviews, must emit nothing. The comment above `_FIRST_PASS_DISQUALIFYING_EVENTS` must still mention \"rework\" and \"first\" and \"clean\" (tests/unit/test_plan_rollup_uncorrelated.py reads it).\n\nAUTHORIZED EDITS TO EXISTING TESTS - make exactly these 7, nothing else. Each old assertion pinned the OLD contract; the behaviour change is the one requested above. State this justification in the commit body: \"Existing tests modified because review_story now notifies review_changes_requested on every bounce, and that event is deliberately both a rework and a first-pass-disqualifying event. (1)(2) The disjointness assertions encoded 'rework events never disqualify', a rationale for environment-caused rework that does not apply to a reviewer bounce; the new expected behaviour is that every rework event except review_changes_requested stays non-disqualifying. (3)(4) The budget-exhausting bounce is also a bounce, so the park path now notifies twice (bounce, then park) instead of once. (5)-(7) These three stubs predate any event= keyword on this path and now receive one, so they accept **k.\"\n\n----- TEST EDIT 1 - tests/unit/test_plan_rollup_uncorrelated.py: OLD (must match exactly once) -----\ndef test_rework_events_are_not_first_pass_disqualifying():\n    assert story_metrics._REWORK_EVENTS.isdisjoint(\n        story_metrics._FIRST_PASS_DISQUALIFYING_EVENTS\n    )\n----- NEW -----\ndef test_rework_events_are_not_first_pass_disqualifying():\n    # review_changes_requested is the one deliberate exception: a reviewer bounce\n    # is a dirty first pass (see the comment above the disqualifying set).\n    assert (story_metrics._REWORK_EVENTS - {\"review_changes_requested\"}).isdisjoint(\n        story_metrics._FIRST_PASS_DISQUALIFYING_EVENTS\n    )\n----- end -----\n\n----- TEST EDIT 2 - tests/unit/test_watchdog_kill_rework_metrics.py: OLD (must match exactly once) -----\n    assert EVENT in story_metrics._REWORK_EVENTS\n    assert story_metrics._REWORK_EVENTS.isdisjoint(\n        story_metrics._FIRST_PASS_DISQUALIFYING_EVENTS\n    )\n----- NEW -----\n    assert EVENT in story_metrics._REWORK_EVENTS\n    # review_changes_requested is the one deliberate exception (a reviewer bounce\n    # is a dirty first pass); every other rework event must stay non-disqualifying.\n    assert (story_metrics._REWORK_EVENTS - {\"review_changes_requested\"}).isdisjoint(\n        story_metrics._FIRST_PASS_DISQUALIFYING_EVENTS\n    )\n----- end -----\n\n----- TEST EDIT 3 - tests/unit/test_pipeline_mcp_server_mark_done_and_patch.py: OLD (must match exactly once) -----\n    assert len(notes) == 1 and \"S1\" in notes[0]\n----- NEW -----\n    # The bounce that exhausts the budget also notifies review_changes_requested,\n    # ahead of the park notice.\n    assert len(notes) == 2 and all(\"S1\" in note for note in notes)\n    assert \"parked\" in notes[-1]\n----- end -----\n\n----- TEST EDIT 4 - tests/unit/test_review_orchestrator_park_event.py: OLD (must match exactly once) -----\n    assert len(calls) == 1\n    assert calls[-1][\"kwargs\"].get(\"event\") == \"story_parked\"\n    on_disk = _read_story(plan_dir, \"pe\", \"S1\")\n    assert on_disk[\"status\"] == \"parked\"\n    assert on_disk[\"parked_reason\"] == (\n        \"rework budget exhausted after 1 review cycles\"\n----- NEW -----\n    assert len(calls) == 2\n    assert calls[0][\"kwargs\"].get(\"event\") == \"review_changes_requested\"\n    assert calls[-1][\"kwargs\"].get(\"event\") == \"story_parked\"\n    on_disk = _read_story(plan_dir, \"pe\", \"S1\")\n    assert on_disk[\"status\"] == \"parked\"\n    assert on_disk[\"parked_reason\"] == (\n        \"rework budget exhausted after 1 review cycles\"\n----- end -----\n\nTEST EDITS 5-7 - tests/unit/test_pipeline_mcp_server_reset_and_stepcap.py\n`monkeypatch.setattr(p, \"_notify_user\", lambda *a: None)` appears 15 times in this file. Change it in EXACTLY these three tests and NO other (the other 12 tests pass today and must keep the old stub):\n  - test_review_story_genuine_request_changes_still_increments_rework\n  - test_advance_pipeline_does_not_report_genuine_verdict_as_deferred\n  - test_review_story_deferred_count_resets_on_genuine_verdict\nIn each, the line\n    monkeypatch.setattr(p, \"_notify_user\", lambda *a: None)\nbecomes\n    monkeypatch.setattr(p, \"_notify_user\", lambda *a, **k: None)\nCheck: `git diff origin/master -- tests/unit/test_pipeline_mcp_server_reset_and_stepcap.py` must show exactly 3 lines removed and 3 added, all of those two lines.\n\nWhat the fixture grades (implement exactly this): the real `review_story` emits exactly one `review_changes_requested` per spent rework attempt, carrying `story_key` (plus `correlation_id` and `attempt` only when the story has a correlation id, and neither key otherwise); a scope-gate bounce emits both `scope_gate_failed` and `review_changes_requested` once each; an inconclusive review emits none and spends no attempt; the budget-exhausting bounce emits it AND `story_parked`; in the metrics one bounce is 1 rework cycle, cost 2, `first_pass_clean` False; a merge with no bounce stays clean; `scope_gate_failed` alone counts nothing; `scope_gate_failed` + the new event counts once; the event is in both sets and every other rework event stays non-disqualifying; the event is not in `DEFAULT_OUTBOX_EVENTS`; REFERENCE.md names it.\n\nSURVIVORS (do not change)\n- The four pre-existing `_REWORK_EVENTS` members plus `dispatch_watchdog_timeout`, and the four pre-existing disqualifying members; the tally loop in `compute_story_metrics`.\n- Every other assertion in the seven edited tests, and every other test in those files.\n- The `scope_gate_failed` emit and the rest of the REFERENCE.md gate paragraph.\n\nSUCCESS CRITERIA\n- Full suite green and `.venv/bin/python -m ruff check .` clean. Report the counts.\n- `git diff origin/master --stat` touches exactly: pipeline/review_orchestrator.py, pipeline/story_metrics.py, REFERENCE.md, tests/unit/test_review_changes_requested_metrics.py, and the five existing test files named above (tests/unit/test_plan_rollup_uncorrelated.py, tests/unit/test_watchdog_kill_rework_metrics.py, tests/unit/test_pipeline_mcp_server_mark_done_and_patch.py, tests/unit/test_review_orchestrator_park_event.py, tests/unit/test_pipeline_mcp_server_reset_and_stepcap.py).\n- Suggested commit: `feat(metrics): count a reviewer send-back as a rework cycle and a dirty first pass`.\n\nHARD CONSTRAINTS (read before touching anything)\n- Do NOT call `restore_file`, `git checkout ... -- <path>`, `git restore`, `git reset --hard` or `git clean` on anything. The harness blocks most of them, and `restore_file` restores from the LAST COMMIT, which is not the base branch. To undo one of your own edits, put the original text back with `str_replace`.\n- Compare against `origin/master`, NEVER the bare `master` ref. The local `master` branch is deliberately stale, so `git diff master` lists files from stories merged since it last moved. Those are not yours: never revert, restore or edit a file that is not on your `files` list.\n- READ-TOOL CAP, NOT FILE CORRUPTION: a `view_file` result that ends in a truncation note means the read tool stopped, the file is intact. Re-read a narrower line range. Never conclude a file is damaged and never rewrite one to \"repair\" it.\n- Use `.venv/bin/python -m pytest` and `.venv/bin/python -m ruff`, never a bare `pytest` or `ruff`.\n- Run the full suite AT MOST TWICE; re-running a green suite burns your steps. When you have enough information, act.\n- `.agent_scratchpad.md` must NEVER be committed. If it shows up tracked, run `git rm --cached .agent_scratchpad.md`.\n- Do not add features, refactor adjacent code, or reformat lines you did not have to change.\n- Commit with Conventional Commits, imperative subject <=72 chars, and a body that explains WHY.\n",
          "acceptance": [
            {
              "path": "tests/unit/test_review_changes_requested_metrics.py",
              "source": "\"\"\"A reviewer sending a story back counts as a rework cycle AND a dirty first pass.\n\nBefore this, a REQUEST_CHANGES verdict left no metric record at all: a story\nbounced three times by review and then merged was reported as zero rework and\nfirst-pass clean. ``review_story`` now notifies once with the structured event\n``review_changes_requested`` every time it spends a rework attempt (the scope\ngate's send-back flows through the same handler, so it emits it too). The\nmetrics count that event as one rework cycle and as a first-pass disqualifier.\n\n``scope_gate_failed`` stays informational: counting it as well would count one\ngate rejection twice, and leaving it out keeps historical sidecars unchanged.\n\nDrives the REAL ``pipeline.server.review_story`` against a real git repo.\nFixture shape is cribbed from ``tests/unit/test_review_scope_gate.py``.\n\"\"\"\n\nimport json\nimport subprocess\nfrom pathlib import Path\n\nimport pytest\n\n# Import pipeline.server FIRST: pipeline.review_orchestrator -> server ->\n# review_orchestrator is a circular import, so importing the orchestrator\n# standalone raises ImportError. Importing the server module first breaks the\n# cycle (same idiom as tests/unit/test_review_scope_gate.py).\nimport pipeline.server as p\nfrom pipeline import concurrency as pcon\nfrom pipeline import persistence as ppers\nfrom pipeline import story_metrics\nfrom pipeline.notification_outbox import DEFAULT_OUTBOX_EVENTS\n\nEVENT = \"review_changes_requested\"\nREPO_ROOT = Path(__file__).resolve().parents[2]\n\n\n@pytest.fixture\ndef plan_dir(tmp_path, monkeypatch):\n    d = tmp_path / \"plans\"\n    d.mkdir()\n    monkeypatch.setattr(p, \"PLAN_DIR\", d)\n    monkeypatch.setattr(ppers, \"PLAN_DIR\", d)\n    monkeypatch.setattr(pcon, \"PLAN_DIR\", d)\n    return d\n\n\ndef _git(repo, *args):\n    subprocess.run([\"git\", *args], cwd=repo, check=True, capture_output=True, text=True)\n\n\ndef _init_repo(tmp_path):\n    \"\"\"A repo on branch ``main`` with a base commit, then branch ``feature``.\"\"\"\n    repo = tmp_path / \"repo\"\n    repo.mkdir()\n    _git(repo, \"init\")\n    _git(repo, \"config\", \"user.email\", \"t@example.com\")\n    _git(repo, \"config\", \"user.name\", \"T\")\n    (repo / \"pipeline\").mkdir()\n    (repo / \"pipeline\" / \"foo.py\").write_text(\"x = 1\\n\")\n    _git(repo, \"add\", \"-A\")\n    _git(repo, \"commit\", \"-m\", \"base\")\n    _git(repo, \"branch\", \"-M\", \"main\")\n    _git(repo, \"checkout\", \"-b\", \"feature\")\n    return repo\n\n\ndef _commit_change(repo, *, out_of_scope):\n    (repo / \"pipeline\" / \"foo.py\").write_text(\"x = 2\\n\")\n    if out_of_scope:\n        (repo / \"stray.py\").write_text(\"y = 1\\n\")\n    _git(repo, \"add\", \"-A\")\n    _git(repo, \"commit\", \"-m\", \"change\")\n\n\ndef _review(plan_dir, monkeypatch, repo, *, reviewer_output, **story_extra):\n    \"\"\"Run review_story with the LLM reviewer stubbed; return (notify kwargs list,\n    the story as stored in the manifest afterwards).\"\"\"\n    calls = []\n    monkeypatch.setattr(p, \"_auto_escalation_enabled\", lambda: False)\n    monkeypatch.setattr(p, \"_notify_user\", lambda *a, **k: calls.append(k))\n    monkeypatch.setattr(p, \"_run_reviewer\", lambda *a, **k: reviewer_output)\n    story = {\n        \"summary\": \"Add thing\",\n        \"status\": \"tests_passed\",\n        \"worktree\": str(repo),\n        \"risk\": \"low\",\n        \"files\": [\"pipeline/foo.py\"],\n        **story_extra,\n    }\n    (plan_dir / \"pe.manifest.json\").write_text(\n        json.dumps({\"epics\": {}, \"stories\": {\"S1\": story}})\n    )\n    p.review_story(\"pe\", \"S1\")\n    saved = json.loads((plan_dir / \"pe.manifest.json\").read_text())\n    return calls, saved[\"stories\"][\"S1\"]\n\n\ndef _events(calls, name):\n    return [c for c in calls if c.get(\"event\") == name]\n\n\n_BOUNCE = \"Add a test.\\nVERDICT: REQUEST_CHANGES\"\n\n\ndef test_llm_reviewer_bounce_emits_one_event_with_the_story_key(plan_dir, monkeypatch):\n    repo = _init_repo(plan_dir.parent)\n    _commit_change(repo, out_of_scope=False)\n\n    calls, _ = _review(plan_dir, monkeypatch, repo, reviewer_output=_BOUNCE)\n\n    bounces = _events(calls, EVENT)\n    assert len(bounces) == 1\n    assert bounces[0][\"story_key\"] == \"S1\"\n    assert \"correlation_id\" not in bounces[0]\n\n\ndef test_bounce_event_carries_correlation_id_and_attempt_when_the_story_has_one(\n    plan_dir, monkeypatch\n):\n    repo = _init_repo(plan_dir.parent)\n    _commit_change(repo, out_of_scope=False)\n\n    calls, _ = _review(\n        plan_dir, monkeypatch, repo, reviewer_output=_BOUNCE,\n        correlation_id=\"c-1\", dispatch_attempts=2,\n    )\n\n    bounce = _events(calls, EVENT)[0]\n    assert bounce[\"correlation_id\"] == \"c-1\"\n    assert bounce[\"attempt\"] == 2\n\n\ndef test_scope_gate_bounce_emits_both_events_once_each(plan_dir, monkeypatch):\n    repo = _init_repo(plan_dir.parent)\n    _commit_change(repo, out_of_scope=True)\n\n    calls, _ = _review(plan_dir, monkeypatch, repo, reviewer_output=_BOUNCE)\n\n    assert len(_events(calls, \"scope_gate_failed\")) == 1\n    assert len(_events(calls, EVENT)) == 1\n\n\ndef test_inconclusive_review_emits_no_bounce_event_and_spends_no_attempt(\n    plan_dir, monkeypatch\n):\n    repo = _init_repo(plan_dir.parent)\n    _commit_change(repo, out_of_scope=False)\n\n    calls, story = _review(\n        plan_dir, monkeypatch, repo, reviewer_output=\"Looks plausible, no verdict given.\"\n    )\n\n    assert _events(calls, EVENT) == []\n    assert story.get(\"rework_attempts\", 0) == 0\n\n\ndef test_the_bounce_that_exhausts_the_rework_budget_still_emits_the_event(\n    plan_dir, monkeypatch\n):\n    repo = _init_repo(plan_dir.parent)\n    _commit_change(repo, out_of_scope=False)\n\n    calls, story = _review(\n        plan_dir, monkeypatch, repo, reviewer_output=_BOUNCE, rework_attempts=99\n    )\n\n    assert story[\"status\"] == \"parked\"\n    assert len(_events(calls, EVENT)) == 1\n    assert len(_events(calls, \"story_parked\")) == 1\n\n\ndef _metrics(*events):\n    records = [{\"event\": name, \"story_key\": \"S1\"} for name in events]\n    return story_metrics.compute_story_metrics(records)[\"S1\"]\n\n\ndef test_a_bounce_counts_one_rework_cycle_and_one_extra_cost():\n    group = _metrics(EVENT, \"story_merged\")\n    assert group[\"rework_cycles\"] == 1\n    assert group[\"cost\"] == 2\n\n\ndef test_a_bounce_is_a_dirty_first_pass():\n    group = _metrics(EVENT, \"story_merged\")\n    assert group[\"disqualifying_events\"] == 1\n    assert group[\"first_pass_clean\"] is False\n\n\ndef test_a_merge_with_no_bounce_is_still_first_pass_clean():\n    group = _metrics(\"story_merged\")\n    assert group[\"rework_cycles\"] == 0\n    assert group[\"first_pass_clean\"] is True\n\n\ndef test_scope_gate_failed_alone_is_informational_and_not_counted():\n    group = _metrics(\"scope_gate_failed\", \"story_merged\")\n    assert group[\"rework_cycles\"] == 0\n    assert group[\"first_pass_clean\"] is True\n\n\ndef test_a_scope_gate_bounce_is_counted_once_not_twice():\n    group = _metrics(\"scope_gate_failed\", EVENT, \"story_merged\")\n    assert group[\"rework_cycles\"] == 1\n    assert group[\"cost\"] == 2\n\n\ndef test_the_event_is_in_both_metric_sets():\n    assert EVENT in story_metrics._REWORK_EVENTS\n    assert EVENT in story_metrics._FIRST_PASS_DISQUALIFYING_EVENTS\n\n\ndef test_every_other_rework_event_stays_non_disqualifying():\n    assert (story_metrics._REWORK_EVENTS - {EVENT}).isdisjoint(\n        story_metrics._FIRST_PASS_DISQUALIFYING_EVENTS\n    )\n\n\ndef test_the_event_is_never_emailed():\n    assert EVENT not in DEFAULT_OUTBOX_EVENTS.split(\",\")\n\n\ndef test_reference_documents_the_event():\n    assert f\"`{EVENT}`\" in (REPO_ROOT / \"REFERENCE.md\").read_text(encoding=\"utf-8\")\n"
            }
          ],
          "dependencies": [
            "PRH-1"
          ],
          "persona": "software-engineer",
          "risk": "low",
          "backend": "ollama",
          "model": "glm-5.3-flash:cloud",
          "files": [
            "pipeline/review_orchestrator.py",
            "pipeline/story_metrics.py",
            "REFERENCE.md"
          ]
        },
        {
          "key": "PRH-4",
          "summary": "Say '1 story' in the retro completion marker",
          "description": "The retros/PENDING.md completion marker wrote '1 stories' for a one-story plan.",
          "agent_instructions": "Preflight: impact run 2026-09-23 on 78b3f48 (scratch worktree, all four PRH stories' real edits applied together) - 12954 passed / 12 skipped / 0 failed; `ruff check .` clean under ruff 0.16.7 (CI's pinned version). The fixture was verified RED on the unedited code (3 of its 7 tests fail on assertions, 0 import errors; the 4 that pass are regression guards) and green with the real edit.\n\nTARGET IMPLEMENTER: on-device (~20B class). Pin: backend \"ollama\", model \"gpt-oss-20b-high:latest\". One production file (pipeline/ci.py, ~880 lines; the edit is two lines in one small function), no new functions, no documentation change.\n\n[no-new-tests] The acceptance fixture named below is the complete, already-verified suite for this story. It is already in your worktree at the stated path. Do NOT write a second test suite, and do NOT modify, rename or delete that file. It is digest-pinned: any edit to it, even whitespace-only, marks it tampered and blocks the story. Never run `ruff format`, black or `ruff check --fix` over it or over any file. Use plain `.venv/bin/python -m ruff check .` to check hygiene. Commit the fixture together with your change (`git add` it) so it lands in the PR.\nFixture: tests/unit/test_retro_pending_story_count_grammar.py (7 tests).\n\nWHY (the defect)\nWhen a plan finishes, the pipeline appends a completion marker to retros/PENDING.md. It always wrote `N stories`, so a one-story plan read `completed <date>, 1 stories`. It must read `1 story`; every other count (including 0) stays `N stories`.\n\nWHAT TO BUILD (1 anchored edit, 1 file). Apply it with `str_replace`, using the OLD text exactly as shown. The `\\u2014` is the six literal characters backslash-u-2-0-1-4 inside the f-string, not an em dash: copy it as written.\n\n----- EDIT 1 - pipeline/ci.py, inside `_record_retro_pending`: OLD (must match exactly once) -----\n    with RETRO_PENDING_PATH.open(\"a\") as f:  # noqa: F821\n        f.write(f\"- {plan_name} \\u2014 completed {date}, {story_count} stories\\n\")\n----- NEW -----\n    noun = \"story\" if story_count == 1 else \"stories\"\n    with RETRO_PENDING_PATH.open(\"a\") as f:  # noqa: F821\n        f.write(f\"- {plan_name} \\u2014 completed {date}, {story_count} {noun}\\n\")\n----- end -----\n\nWhat the fixture grades: counts 0, 1, 2 and 11 produce `stories`, `story`, `stories`, `stories`; a one-story marker never contains `1 stories`; the real merge path (`_maybe_record_retro`) writes the singular marker; a second call for the same plan appends nothing (the dedup by plan name is unchanged).\n\nSURVIVORS (do not change)\n- The dedup check (`marker = f\"- {plan_name} \"` and the `startswith` test), the date, the em dash, the `# noqa: F821` comments, and the `types.FunctionType` rebinding at the bottom of pipeline/ci.py.\n- retros/PENDING.md itself: the fixture points the writer at a throwaway file. Never edit the real one.\n- tests/unit/test_retro_pending_merge_paths.py and tests/acceptance_retro_pending_merge_paths.py pass UNMODIFIED.\n\nSUCCESS CRITERIA\n- `.venv/bin/python -m pytest -q tests/unit/test_retro_pending_story_count_grammar.py tests/unit/test_retro_pending_merge_paths.py` is green.\n- Full suite green and `.venv/bin/python -m ruff check .` clean. Report the counts.\n- `git diff origin/master --stat` touches exactly: pipeline/ci.py, tests/unit/test_retro_pending_story_count_grammar.py.\n- Suggested commit: `fix(retros): say 1 story in the completion marker`.\n\nHARD CONSTRAINTS (read before touching anything)\n- Do NOT call `restore_file`, `git checkout ... -- <path>`, `git restore`, `git reset --hard` or `git clean` on anything. The harness blocks most of them, and `restore_file` restores from the LAST COMMIT, which is not the base branch. To undo one of your own edits, put the original text back with `str_replace`.\n- Compare against `origin/master`, NEVER the bare `master` ref. The local `master` branch is deliberately stale, so `git diff master` lists files from stories merged since it last moved. Those are not yours: never revert, restore or edit a file that is not on your `files` list.\n- READ-TOOL CAP, NOT FILE CORRUPTION: a `view_file` result that ends in a truncation note means the read tool stopped, the file is intact. Re-read a narrower line range. Never conclude a file is damaged and never rewrite one to \"repair\" it.\n- Use `.venv/bin/python -m pytest` and `.venv/bin/python -m ruff`, never a bare `pytest` or `ruff`.\n- Run the full suite AT MOST TWICE; re-running a green suite burns your steps. When you have enough information, act.\n- `.agent_scratchpad.md` must NEVER be committed. If it shows up tracked, run `git rm --cached .agent_scratchpad.md`.\n- Do not add features, refactor adjacent code, or reformat lines you did not have to change.\n- Commit with Conventional Commits, imperative subject <=72 chars, and a body that explains WHY.\n",
          "acceptance": [
            {
              "path": "tests/unit/test_retro_pending_story_count_grammar.py",
              "source": "\"\"\"retros/PENDING.md completion markers must use the right noun for the count.\n\nThe marker written when a plan finishes read ``completed <date>, 1 stories``\nfor a one-story plan. It must read ``1 story`` and keep ``N stories`` for every\nother count (including 0). The marker's dedup-by-plan-name behavior is\nunchanged.\n\nAssertions read the throwaway PENDING.md these tests point the writer at;\nthey never touch the real, shared retros/PENDING.md.\n\"\"\"\n\nimport importlib\nimport re\n\nimport pytest\n\np = importlib.import_module(\"pipeline.server\")\nci = importlib.import_module(\"pipeline.ci\")\nmerge = importlib.import_module(\"pipeline.merge\")\n\n_DATE = r\"\\d{4}-\\d{2}-\\d{2}\"\n\n\ndef _expected_marker(plan, count, noun):\n    prefix = re.escape(f\"- {plan} — completed \")\n    return re.compile(prefix + _DATE + re.escape(f\", {count} {noun}\"))\n\n\n@pytest.mark.parametrize(\n    (\"count\", \"noun\"),\n    [(0, \"stories\"), (1, \"story\"), (2, \"stories\"), (11, \"stories\")],\n)\ndef test_marker_uses_the_right_noun_for_the_story_count(tmp_path, monkeypatch, count, noun):\n    pending = tmp_path / \"PENDING.md\"\n    monkeypatch.setattr(p, \"RETRO_PENDING_PATH\", pending, raising=False)\n\n    ci._record_retro_pending(\"grammar-plan\", count)\n\n    assert _expected_marker(\"grammar-plan\", count, noun).fullmatch(pending.read_text().strip())\n\n\ndef test_a_one_story_marker_never_says_1_stories(tmp_path, monkeypatch):\n    pending = tmp_path / \"PENDING.md\"\n    monkeypatch.setattr(p, \"RETRO_PENDING_PATH\", pending, raising=False)\n\n    ci._record_retro_pending(\"grammar-plan\", 1)\n\n    assert \"1 stories\" not in pending.read_text()\n\n\ndef test_the_real_merge_path_writes_the_singular_marker(tmp_path, monkeypatch):\n    pending = tmp_path / \"PENDING.md\"\n    monkeypatch.setattr(p, \"RETRO_PENDING_PATH\", pending, raising=False)\n    manifest = {\n        \"repo_root\": str(p.PIPELINE_SELF_REPO_ROOT),\n        \"stories\": {\"a\": {\"status\": \"done\"}},\n    }\n\n    merge._maybe_record_retro(\"one-story-plan\", manifest)\n\n    assert _expected_marker(\"one-story-plan\", 1, \"story\").fullmatch(pending.read_text().strip())\n\n\ndef test_a_second_call_for_the_same_plan_appends_nothing(tmp_path, monkeypatch):\n    pending = tmp_path / \"PENDING.md\"\n    monkeypatch.setattr(p, \"RETRO_PENDING_PATH\", pending, raising=False)\n\n    ci._record_retro_pending(\"grammar-plan\", 1)\n    ci._record_retro_pending(\"grammar-plan\", 1)\n\n    assert len(pending.read_text().splitlines()) == 1\n"
            }
          ],
          "persona": "software-engineer",
          "risk": "low",
          "backend": "ollama",
          "model": "gpt-oss-20b-high:latest",
          "files": [
            "pipeline/ci.py"
          ]
        }
      ]
    }
  ]
}