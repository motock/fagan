PROGRESS: 3/10
Step 1 done — scratchpad initialized.
Step 2: Full-suite run #1: 17 failed, 3782 passed. Failures include the two target CI-status tests plus many others (test_usage_gate, test_merge_ci_pending_production_path, test_final_rework_escalation). This is a broad leak affecting many tests.
Step 3: test_pipeline_mcp_server.py alone: 724 passed (file-isolation passes). The two target CI-status tests pass in isolation. So leak comes from another test file.

Step 2 (re-run, this session): `pytest -q` full suite reproduces deterministically. All `test_ci_status_*` tests fail with `assert 'pass' == '<expected>'` — i.e. `_ci_status` returns "pass" (the `PIPELINE_MERGE_CI_GATE` disabled branch) instead of the expected state. `pytest -q -x` first failure is `test_final_rework_escalation_noop_on_oracle_cap_of_one`: `assert p.REWORK_MAX_ATTEMPTS_ORACLE == 1` got `3`.

KEY OBSERVATION: Both `PIPELINE_MERGE_CI_GATE` (pipeline/ci.py:31) and `REWORK_MAX_ATTEMPTS_ORACLE` (pipeline/config.py:204) are module-level constants read from `PIPELINE_*` env vars at import time. Both are wrong at runtime (False / 3). The conftest `_isolate_environ` autouse fixture deletes PIPELINE_* env vars before each test and restores after, but does NOT reset the module-level constants that were read at import time. So a test that sets a PIPELINE_* env var and reloads the module leaks the constant for the rest of the process.

Next: find which test sets a PIPELINE_* env var and reloads pipeline.config / pipeline.ci (or app.backend which imports them), leaving the module constant stale.
