{
  "repo_root": "/Users/jessecarroll/git/fagan",
  "epics": [
    {
      "summary": "Cold-importable pipeline modules (Phase 1)",
      "stories": [
        {
          "key": "PCI-1",
          "summary": "Stop build_detect importing pipeline.server so seven modules import cold",
          "description": "pipeline.build_detect eagerly imported pipeline.server 'for its priming side effect', closing the cycle advance -> dispatch -> build_detect -> server -> advance. Seven modules could only be imported after the server. Remove the priming import and the two workarounds it spawned (dispatch's end-of-module import, companion_server's _ColdOracleGateImport meta_path hook). Phase 2 (story_status, review_orchestrator, server_tools_lifecycle) is a separate plan.",
          "agent_instructions": "Preflight: impact run 2026-09-24 on 4696ef5 (scratch worktree, the real edit applied) - 12976 passed / 12 skipped / 0 failed (baseline 12962 plus the 14 fixture tests); `ruff check .` clean under ruff 0.16.8 (CI's pinned version). The fixture was verified RED on the unedited code (10 of its 14 tests fail on assertions carrying the ImportError text, 0 import errors in the test file itself; the 4 that pass are controls) and green with the real edit. One pre-existing test conflicts with the change and is PRE-AUTHORIZED below (tests/unit/test_companion_server.py::test_cold_oracle_gate_import_hook_primes_and_delegates: name and docstring only, assertions byte-identical).\n\nTARGET IMPLEMENTER: cloud open-source (backend \"ollama\", model \"glm-5.3-flash:cloud\"). Three production files (pipeline/build_detect.py ~998 lines, pipeline/companion_server.py ~178, pipeline/dispatch.py ~991), all edits are deletions or one moved import line, no new functions, no documentation change.\n\n[no-new-tests] The acceptance fixture named below is the complete, already-verified suite for this story. It is already in your worktree at the stated path. Do NOT write a second test suite, and do NOT modify, rename or delete that file. It is digest-pinned: any edit to it, even whitespace-only, marks it tampered and blocks the story. Never run `ruff format`, black or `ruff check --fix` over it or over any file. Use plain `.venv/bin/python -m ruff check .` to check hygiene. Commit the fixture together with your change (`git add` it) so it lands in the PR.\nFixture: tests/unit/test_pipeline_cold_imports.py (14 tests, each probe runs in a fresh subprocess because tests/unit/conftest.py pre-imports pipeline.server and would hide the defect in-process).\n\nWHY (the defect)\n`import pipeline.advance` in a fresh interpreter dies with ImportError. The cycle is advance -> dispatch (end-of-module `from .build_detect import ...`) -> build_detect (eager `import pipeline.server as _server`, \"priming side effect only\") -> server (`from pipeline.advance import ...`) -> the half-initialized advance. The same edge breaks seven modules cold: advance, detached_grade, ingest, oracle_gate, plan_conflict_ruling, repo_health, triage. Three workarounds grew around it: the priming import itself, dispatch's end-of-module import, and the `_ColdOracleGateImport` sys.meta_path hook in pipeline/companion_server.py that primes build_detect for a cold `import pipeline.oracle_gate`. Nothing reads pipeline.server from sys.modules, so the priming import has no consumer; removing it fixes the cycle and makes the other two workarounds dead.\n\nWHAT TO DO (one step, one short command)\nThe whole change is already written as a script that lives OUTSIDE the repository, so there is nothing to type or copy. Run exactly this, from the repository root, in one bash call:\n\n.venv/bin/python /Users/jessecarroll/.claude/plans/pipeline-cold-imports.PCI-1.apply.py\n\nIt (1) deletes the eager pipeline.server import block from pipeline/build_detect.py, (2) deletes the `_ColdOracleGateImport` and `_AlreadyInitializedLoader` classes, their `sys.meta_path.insert`, and the now-unused `import importlib.util` / `import sys` from pipeline/companion_server.py, (3) moves `from .build_detect import detect_test_command, failed_node_ids  # noqa: F401` from the end of pipeline/dispatch.py to the top import block (before `from .config import WORKTREE_SCOPE_RULE`), and (4) applies the pre-authorized test rename below. It compiles every file and prints `OK 4` on success. Every assertion runs before any write, so it never leaves a half-edited file. Do NOT open, retype, paste or modify the script, and do NOT hand-edit any of the four files.\n\nIf it printed `OK 4`, go to VERIFY. If it raised an AssertionError or SyntaxError, do NOT improvise and do NOT hand-edit any file: run `git status --short` and `git diff origin/master --stat`, and report the exact message.\n\nPRE-AUTHORIZED EXISTING-TEST EDIT (CLAUDE.md Step 4 justification, put it in the commit body)\nFile tests/unit/test_companion_server.py, test `test_cold_oracle_gate_import_hook_primes_and_delegates` becomes `test_cold_oracle_gate_import_after_companion_import_delegates`, and its docstring is reworded. The old name and docstring describe a `_ColdOracleGateImport` hook that this change removes on purpose; the hook existed only to prime the cycle. The assertions and the subprocess probe are UNCHANGED, so the test still proves that a cold `import pipeline.oracle_gate` after importing only the companion succeeds and delegates. Nothing is loosened, deleted or weakened. The apply script performs this edit; do not make any other change to that file.\n\nVERIFY (once each)\n1. `git diff origin/master --stat` lists exactly pipeline/build_detect.py, pipeline/companion_server.py, pipeline/dispatch.py, tests/unit/test_companion_server.py and tests/unit/test_pipeline_cold_imports.py.\n2. The tests named under SUCCESS CRITERIA are green.\n3. Commit everything in ONE step so the branch head carries the change: `git add pipeline/build_detect.py pipeline/companion_server.py pipeline/dispatch.py tests/unit/test_companion_server.py tests/unit/test_pipeline_cold_imports.py && git commit -m \"fix(imports): stop build_detect importing pipeline.server so modules import cold\"`.\n\nWhat the fixture grades (implement exactly this): each of advance, detached_grade, ingest, oracle_gate, plan_conflict_ruling, repo_health and triage imports in a fresh interpreter; importing pipeline.build_detect does not load pipeline.server; build_detect still exposes a callable `_run_lint_gate`; scheduler_daemon, app.pipeline_mcp_server and \"server first, then everything\" still import; companion_server has no `_ColdOracleGateImport` attribute and installs no import hook on sys.meta_path; pipeline/dispatch.py imports build_detect at the top of the module, before its first def or class.\n\nSURVIVORS (do not change)\n- Phase 2 is a SEPARATE plan: `story_status`, `review_orchestrator` and `server_tools_lifecycle` still fail a standalone import because they cycle directly with the server hub (`from .server import ...`). Do NOT touch them and do NOT try to fix them.\n- The \"Import pipeline.server FIRST\" comments and `import pipeline.server` lines in other tests stay exactly as they are.\n- `_run_lint_gate_lazy` in pipeline/build_detect.py and `_ServerRef` in pipeline/service.py and pipeline/advance.py: these are the intended cycle-breakers.\n- Everything else in the three production files, including the rest of the companion's tool registration and every other import in pipeline/dispatch.py.\n- Every other test in tests/unit/test_companion_server.py and every other existing test: they pass UNMODIFIED.\n\nSUCCESS CRITERIA\n- `.venv/bin/python -m pytest -q tests/unit/test_pipeline_cold_imports.py tests/unit/test_companion_server.py` is green (41 tests).\n- Full suite green and `.venv/bin/python -m ruff check .` clean. Report the counts.\n- `git diff origin/master --stat` touches exactly: pipeline/build_detect.py, pipeline/companion_server.py, pipeline/dispatch.py, tests/unit/test_companion_server.py, tests/unit/test_pipeline_cold_imports.py (about +11/-105 excluding the fixture).\n- Suggested commit: `fix(imports): stop build_detect importing pipeline.server so modules import cold`.\n\nHARD CONSTRAINTS (read before touching anything)\n- Never run `git checkout <ref>`, `git switch --detach`, `git checkout --detach`, `git reset --hard` or `git clean` on this worktree. Stay on the branch you were given.\n- Do NOT call `restore_file`, `git checkout ... -- <path>` or `git restore` on anything. The harness blocks most of them, and `restore_file` restores from the LAST COMMIT, which is not the base branch. If an edit went wrong, stop and report it; do not try to undo it by hand.\n- Compare against `origin/master`, NEVER the bare `master` ref. The local `master` branch is deliberately stale, so `git diff master` lists files from stories merged since it last moved. Those are not yours: never revert, restore or edit a file that is not on your `files` list.\n- READ-TOOL CAP, NOT FILE CORRUPTION: a `view_file` result that ends in a truncation note means the read tool stopped, the file is intact. Re-read a narrower line range. Never conclude a file is damaged and never rewrite one to \"repair\" it.\n- Use `.venv/bin/python -m pytest` and `.venv/bin/python -m ruff`, never a bare `pytest` or `ruff`.\n- Run the full suite AT MOST TWICE; re-running a green suite burns your steps. When you have enough information, act.\n- `.agent_scratchpad.md` must NEVER be committed. If it shows up tracked, run `git rm --cached .agent_scratchpad.md`.\n- Do not add features, refactor adjacent code, or reformat lines you did not have to change.\n- Do NOT use `str_replace` or `replace_lines` on pipeline/build_detect.py, pipeline/companion_server.py, pipeline/dispatch.py or tests/unit/test_companion_server.py. The command under WHAT TO DO is the only way these files change.\n- Commit with Conventional Commits, imperative subject <=72 chars, and a body that explains WHY.\n",
          "acceptance": [
            {
              "path": "tests/unit/test_pipeline_cold_imports.py",
              "source": "\"\"\"pipeline modules must import cold, in a fresh interpreter, without help.\n\npipeline.build_detect used to import pipeline.server eagerly at module level. That\nedge closed a cycle (advance -> dispatch -> build_detect -> server -> advance), so\nseven modules could only be imported after pipeline.server had already been\nimported, and three workarounds grew around it: the end-of-module build_detect\nimport in pipeline.dispatch, the _ColdOracleGateImport meta_path hook in\npipeline.companion_server, and an \"import pipeline.server first\" convention in tests.\n\ntests/unit/conftest.py pre-imports pipeline.server, which hides every one of these\nfailures in-process, so each probe here runs in a subprocess.\n\"\"\"\n\nimport ast\nimport subprocess\nimport sys\nfrom pathlib import Path\n\nimport pytest\n\nREPO_ROOT = Path(__file__).resolve().parents[2]\n\n\ndef _probe(code):\n    return subprocess.run(\n        [sys.executable, \"-c\", code],\n        capture_output=True,\n        text=True,\n        check=False,\n        cwd=REPO_ROOT,\n    )\n\n\n@pytest.mark.parametrize(\n    \"module\",\n    [\n        \"advance\",\n        \"detached_grade\",\n        \"ingest\",\n        \"oracle_gate\",\n        \"plan_conflict_ruling\",\n        \"repo_health\",\n        \"triage\",\n    ],\n)\ndef test_module_imports_cold_in_a_fresh_interpreter(module):\n    result = _probe(f\"import pipeline.{module}\")\n    assert result.returncode == 0, result.stderr\n\n\ndef test_importing_build_detect_does_not_load_the_server():\n    result = _probe(\"import sys, pipeline.build_detect; print('pipeline.server' in sys.modules)\")\n    assert result.returncode == 0, result.stderr\n    assert result.stdout.strip() == \"False\"\n\n\ndef test_build_detect_still_exposes_a_callable_lint_gate():\n    result = _probe(\"import pipeline.build_detect as b; print(callable(b._run_lint_gate))\")\n    assert result.returncode == 0, result.stderr\n    assert result.stdout.strip() == \"True\"\n\n\n@pytest.mark.parametrize(\n    \"code\",\n    [\n        \"import pipeline.scheduler_daemon\",\n        \"import app.pipeline_mcp_server\",\n        \"import pipeline.server, pipeline.advance, pipeline.triage, pipeline.story_status, pipeline.review_orchestrator\",\n    ],\n    ids=[\"scheduler_daemon\", \"mcp_server\", \"server_first_then_everything\"],\n)\ndef test_entry_points_still_import(code):\n    result = _probe(code)\n    assert result.returncode == 0, result.stderr\n\n\ndef test_companion_installs_no_import_hook():\n    result = _probe(\n        \"import sys, pipeline.companion_server as cs; \"\n        \"print(hasattr(cs, '_ColdOracleGateImport'), \"\n        \"any(type(f).__name__ == '_ColdOracleGateImport' for f in sys.meta_path))\"\n    )\n    assert result.returncode == 0, result.stderr\n    assert result.stdout.strip() == \"False False\"\n\n\ndef test_dispatch_imports_build_detect_at_the_top_of_the_module():\n    tree = ast.parse((REPO_ROOT / \"pipeline\" / \"dispatch.py\").read_text())\n    index_of_build_detect_import = next(\n        i\n        for i, node in enumerate(tree.body)\n        if isinstance(node, ast.ImportFrom) and node.module == \"build_detect\" and node.level == 1\n    )\n    index_of_first_definition = next(\n        i for i, node in enumerate(tree.body) if isinstance(node, (ast.FunctionDef, ast.ClassDef))\n    )\n    assert index_of_build_detect_import < index_of_first_definition\n"
            }
          ],
          "persona": "software-engineer",
          "risk": "low",
          "backend": "ollama",
          "model": "glm-5.3-flash:cloud",
          "files": [
            "pipeline/build_detect.py",
            "pipeline/companion_server.py",
            "pipeline/dispatch.py"
          ]
        }
      ]
    }
  ]
}
