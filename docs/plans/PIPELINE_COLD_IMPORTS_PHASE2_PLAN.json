{
  "repo_root": "/Users/jessecarroll/git/fagan",
  "epics": [
    {
      "summary": "Cold-importable pipeline modules (Phase 2)",
      "stories": [
        {
          "key": "PCI2-1",
          "summary": "Import pipeline.server lazily inside review_story so review_orchestrator imports cold",
          "description": "review_orchestrator imported pipeline.server at module level for one call site, so a cold import re-entered it half-initialized. Move the import into the one function that uses it.",
          "agent_instructions": "Preflight: impact run 2026-09-24 on 931099f (scratch worktree, the real edit applied) - 12980 passed / 12 skipped / 0 failed (baseline 12976 plus the 4 fixture tests); `ruff check .` clean under ruff 0.16.8 (CI's pinned version). The fixture was verified RED on the unedited code (3 of its 4 tests fail on assertions carrying the ImportError text; the 1 that passes is the server-first control; 0 import errors in the test file itself) and green with the real edit. 0 pre-existing tests conflict with the change: no existing test is edited.\n\nTARGET IMPLEMENTER: cloud open-source (backend \"ollama\", model \"glm-5.3-flash:cloud\"). One production file (pipeline/review_orchestrator.py, ~900 lines); the whole change is one deleted module-level import and one function-local import. No new functions, no documentation change.\n\n[no-new-tests] The acceptance fixture named below is the complete, already-verified suite for this story. It is already in your worktree at the stated path. Do NOT write a second test suite, and do NOT modify, rename or delete that file. It is digest-pinned: any edit to it, even whitespace-only, marks it tampered and blocks the story. Never run `ruff format`, black or `ruff check --fix` over it or over any file. Use plain `.venv/bin/python -m ruff check .` to check hygiene. Commit the fixture together with your change (`git add` it) so it lands in the PR.\nFixture: tests/unit/test_review_orchestrator_cold_import.py (4 tests). Every probe runs in a fresh subprocess because tests/unit/conftest.py pre-imports pipeline.server and would hide the defect in-process.\n\nWHY (the defect)\n`import pipeline.review_orchestrator` in a fresh interpreter dies with ImportError. The module runs `import pipeline.server as _server` at module level for exactly one call site (`_server._verify_reviewer_auto_fix(...)` inside `review_story`), and pipeline.server imports this module, so a cold import re-enters it half-initialized (\"cannot import name '_original_review_story' from partially initialized module\"). Every other server-sourced name in the file is already a lazy `_ServerRef`.\n\nWHAT TO DO (one step, one short command)\nThe whole change is already written as a script that lives OUTSIDE the repository, so there is nothing to type or copy. Run exactly this, from the repository root, in one bash call:\n\n.venv/bin/python /Users/jessecarroll/.claude/plans/pipeline-cold-imports-phase2.PCI2-1.apply.py\n\nIt (1) deletes the module-level `import pipeline.server as _server` and (2) adds the same import as a function-local statement immediately before the one call site inside the `APPROVE_WITH_FIX` branch of `review_story`. It compiles every file and prints `OK 1` on success. It also checks each result against a hash of the verified reference and every assertion runs before any write, so it never leaves a half-edited file. Do NOT open, retype, paste or modify the script, and do NOT hand-edit any production file.\n\nIf it printed `OK 1`, go to VERIFY. If it raised an AssertionError or SyntaxError, do NOT improvise and do NOT hand-edit any file: run `git status --short` and `git diff origin/master --stat`, and report the exact message.\n\nVERIFY (once each)\n1. `git diff origin/master --stat` lists exactly pipeline/review_orchestrator.py and tests/unit/test_review_orchestrator_cold_import.py.\n2. The tests named under SUCCESS CRITERIA are green.\n3. Commit everything in ONE step so the branch head carries the change: `git add pipeline/review_orchestrator.py tests/unit/test_review_orchestrator_cold_import.py && git commit -m \"fix(imports): import pipeline.server lazily in review_orchestrator\"`.\n\nWhat the fixture grades (implement exactly this): `import pipeline.review_orchestrator` succeeds in a fresh interpreter and does not load pipeline.server; importing the orchestrator first and then pipeline.server still works; after `import pipeline.server`, the server's `_original_review_story` and `_verify_reviewer_auto_fix` are the orchestrator's own objects.\n\nSURVIVORS (do not change)\n- Everything else in pipeline/review_orchestrator.py, especially every `_ServerRef(...)` binding and the body of `review_story`. The call still goes through `pipeline.server` on purpose, so tests that patch `pipeline.server._verify_reviewer_auto_fix` keep landing.\n- pipeline/server.py, pipeline/review_autofix.py and every other module.\n- Phase 2's other modules (`server_tools_lifecycle`, `story_status`, `ci`) are separate stories. Do NOT touch them.\n- Every existing test passes UNMODIFIED.\n\nSUCCESS CRITERIA\n- `.venv/bin/python -m pytest -q tests/unit/test_review_orchestrator_cold_import.py tests/unit/test_ld90_review_orchestrator_attribution.py tests/unit/test_review_orchestrator_park_event.py tests/unit/test_review_scope_gate.py tests/unit/test_review_changes_requested_metrics.py` is green (32 tests).\n- Full suite green and `.venv/bin/python -m ruff check .` clean. Report the counts.\n- `git diff origin/master --stat` touches exactly: pipeline/review_orchestrator.py, tests/unit/test_review_orchestrator_cold_import.py.\n- Suggested commit: `fix(imports): import pipeline.server lazily in review_orchestrator`.\n\nHARD CONSTRAINTS (read before touching anything)\n- Never run `git checkout <ref>`, `git switch --detach`, `git checkout --detach`, `git reset --hard` or `git clean` on this worktree. Stay on the branch you were given.\n- Do NOT call `restore_file`, `git checkout ... -- <path>` or `git restore` on anything. The harness blocks most of them, and `restore_file` restores from the LAST COMMIT, which is not the base branch. If an edit went wrong, stop and report it; do not try to undo it by hand.\n- Compare against `origin/master`, NEVER the bare `master` ref. The local `master` branch is deliberately stale, so `git diff master` lists files from stories merged since it last moved. Those are not yours: never revert, restore or edit a file that is not on your `files` list.\n- READ-TOOL CAP, NOT FILE CORRUPTION: a `view_file` result that ends in a truncation note means the read tool stopped, the file is intact. Re-read a narrower line range. Never conclude a file is damaged and never rewrite one to \"repair\" it.\n- Use `.venv/bin/python -m pytest` and `.venv/bin/python -m ruff`, never a bare `pytest` or `ruff`.\n- Run the full suite AT MOST TWICE; re-running a green suite burns your steps. When you have enough information, act.\n- `.agent_scratchpad.md` must NEVER be committed. If it shows up tracked, run `git rm --cached .agent_scratchpad.md`.\n- Do not add features, refactor adjacent code, or reformat lines you did not have to change.\n- Do NOT modify, rename or delete any existing test. No existing test needs to change for this story.\n- Do NOT use `str_replace` or `replace_lines` on any production file. The command under WHAT TO DO is the only way the production files change.\n- Commit with Conventional Commits, imperative subject <=72 chars, and a body that explains WHY.\n",
          "acceptance": [
            {
              "path": "tests/unit/test_review_orchestrator_cold_import.py",
              "source": "\"\"\"pipeline.review_orchestrator must import cold, without pulling in the server.\n\nIt used to run ``import pipeline.server as _server`` at module level for a single\ncall site inside review_story. pipeline.server imports this module, so a cold\n``import pipeline.review_orchestrator`` re-entered it half-initialized and died\nwith an ImportError. tests/unit/conftest.py pre-imports pipeline.server, which hides\nthat in-process, so every probe here runs in a subprocess.\n\"\"\"\n\nimport subprocess\nimport sys\nfrom pathlib import Path\n\nREPO_ROOT = Path(__file__).resolve().parents[2]\n\n\ndef _probe(code):\n    return subprocess.run(\n        [sys.executable, \"-c\", code],\n        capture_output=True,\n        text=True,\n        check=False,\n        cwd=REPO_ROOT,\n    )\n\n\ndef test_review_orchestrator_imports_cold_in_a_fresh_interpreter():\n    result = _probe(\"import pipeline.review_orchestrator\")\n    assert result.returncode == 0, result.stderr\n\n\ndef test_importing_review_orchestrator_does_not_load_the_server():\n    result = _probe(\"import sys, pipeline.review_orchestrator; print('pipeline.server' in sys.modules)\")\n    assert result.returncode == 0, result.stderr\n    assert result.stdout.strip() == \"False\"\n\n\ndef test_server_still_re_exports_the_review_orchestrator_names():\n    result = _probe(\n        \"import pipeline.server as p, pipeline.review_orchestrator as r; \"\n        \"print(p._original_review_story is r._original_review_story, \"\n        \"p._verify_reviewer_auto_fix is r._verify_reviewer_auto_fix)\"\n    )\n    assert result.returncode == 0, result.stderr\n    assert result.stdout.strip() == \"True True\"\n\n\ndef test_review_orchestrator_first_then_server_still_imports():\n    result = _probe(\"import pipeline.review_orchestrator, pipeline.server\")\n    assert result.returncode == 0, result.stderr\n"
            }
          ],
          "persona": "software-engineer",
          "risk": "low",
          "backend": "ollama",
          "model": "glm-5.3-flash:cloud",
          "files": [
            "pipeline/review_orchestrator.py"
          ]
        },
        {
          "key": "PCI2-2",
          "summary": "Register the lifecycle MCP tools from pipeline.server so server_tools_lifecycle imports cold",
          "description": "server_tools_lifecycle decorated its tools with a lazy reference to the server's FastMCP instance, which resolved the server at import time and closed a cycle. Make the tools plain functions and register them in pipeline.server.",
          "agent_instructions": "Preflight: impact run 2026-09-24 on 931099f (scratch worktree, the real edit applied) - 12998 passed / 12 skipped / 0 failed (baseline 12976 plus PCI2-1's 4 tests plus this fixture's 18); `ruff check .` clean under ruff 0.16.8 (CI's pinned version). The fixture was verified RED on the unedited code (10 of its 18 tests fail on assertions carrying the ImportError text; the 8 that pass are the server-first registration controls; 0 import errors in the test file itself) and green with the real edit. 0 pre-existing tests conflict with the change: no existing test is edited.\n\nTARGET IMPLEMENTER: cloud open-source (backend \"ollama\", model \"glm-5.3-flash:cloud\"). Two production files (pipeline/server_tools_lifecycle.py ~200 lines and pipeline/server.py ~991 lines). The change deletes 9 lines from the first and swaps one import block for 8 registration lines in the second. No new functions, no documentation change. server.py is near the size cap, so the edit is scripted rather than typed.\n\n[no-new-tests] The acceptance fixture named below is the complete, already-verified suite for this story. It is already in your worktree at the stated path. Do NOT write a second test suite, and do NOT modify, rename or delete that file. It is digest-pinned: any edit to it, even whitespace-only, marks it tampered and blocks the story. Never run `ruff format`, black or `ruff check --fix` over it or over any file. Use plain `.venv/bin/python -m ruff check .` to check hygiene. Commit the fixture together with your change (`git add` it) so it lands in the PR.\nFixture: tests/unit/test_server_tools_lifecycle_cold_import.py (18 tests). Every probe runs in a fresh subprocess because tests/unit/conftest.py pre-imports pipeline.server and would hide the defect in-process.\n\nWHY (the defect)\n`import pipeline.server_tools_lifecycle` in a fresh interpreter dies with ImportError. Its 8 tools are decorated with `@mcp.tool()`, where `mcp` is a lazy `_ModuleRef` to pipeline.server's FastMCP instance. Decorating resolves the server at import time, and pipeline.server imports this module, so a cold import re-enters it half-initialized (\"cannot import name 'advance_all_plans' from partially initialized module\"). Registration belongs in the server, which owns the `mcp` instance: the tools become plain functions and pipeline.server calls `mcp.tool()(fn)` on each. `mcp.tool()` returns the function itself, so `p.review_story` is still the registered object.\n\nWHAT TO DO (one step, one short command)\nThe whole change is already written as a script that lives OUTSIDE the repository, so there is nothing to type or copy. Run exactly this, from the repository root, in one bash call:\n\n.venv/bin/python /Users/jessecarroll/.claude/plans/pipeline-cold-imports-phase2.PCI2-2.apply.py\n\nIt (1) removes the eight `@mcp.tool()` decorators and the `mcp = _ModuleRef(...)` line from pipeline/server_tools_lifecycle.py and rewrites its module docstring to say the tools are plain functions the server registers, and (2) replaces the `from .server_tools_lifecycle import (...)` block near the bottom of pipeline/server.py with `from . import server_tools_lifecycle as _lifecycle_tools` plus eight `name = mcp.tool()(_lifecycle_tools.name)` lines, in the original order. It compiles every file and prints `OK 2` on success. It also checks each result against a hash of the verified reference and every assertion runs before any write, so it never leaves a half-edited file. Do NOT open, retype, paste or modify the script, and do NOT hand-edit any production file.\n\nIf it printed `OK 2`, go to VERIFY. If it raised an AssertionError or SyntaxError, do NOT improvise and do NOT hand-edit any file: run `git status --short` and `git diff origin/master --stat`, and report the exact message.\n\nVERIFY (once each)\n1. `git diff origin/master --stat` lists exactly pipeline/server_tools_lifecycle.py, pipeline/server.py and tests/unit/test_server_tools_lifecycle_cold_import.py.\n2. The tests named under SUCCESS CRITERIA are green.\n3. Commit everything in ONE step so the branch head carries the change: `git add pipeline/server_tools_lifecycle.py pipeline/server.py tests/unit/test_server_tools_lifecycle_cold_import.py && git commit -m \"fix(imports): register the lifecycle MCP tools from pipeline.server\"`.\n\nWhat the fixture grades (implement exactly this): `import pipeline.server_tools_lifecycle` succeeds in a fresh interpreter and does not load pipeline.server; for each of the 8 tools (`request_decision`, `list_decisions`, `review_story`, `advance_pipeline`, `approve_merge`, `pause_plan`, `resume_plan`, `advance_all_plans`), `pipeline.server.<name>` is the lifecycle module's function and is the function registered in `pipeline.server.mcp._tool_manager._tools`, whether the server or the lifecycle module is imported first.\n\nSURVIVORS (do not change)\n- The bodies, signatures and docstrings of all 8 tool functions, and `_service = _ModuleRef(\"pipeline.server\", \"_service\")` in the lifecycle module.\n- Everything else in pipeline/server.py, including the `check_story_status` import and the `pipeline.ci` import block: those belong to a later story (PCI2-3).\n- pipeline/service.py, pipeline/module_ref.py and every other module.\n- The tests that count `def pause_plan` / `def advance_all_plans` definitions across server.py, service.py and the lifecycle module: the definitions do not move, so they stay green.\n- Every existing test passes UNMODIFIED.\n\nSUCCESS CRITERIA\n- `.venv/bin/python -m pytest -q tests/unit/test_server_tools_lifecycle_cold_import.py tests/unit/test_pipeline_service_seam.py tests/unit/test_advance_all_plans_migration.py tests/unit/test_review_story_registry_and_validation.py tests/unit/test_pipeline_cold_imports.py` is green (105 tests).\n- Full suite green and `.venv/bin/python -m ruff check .` clean. Report the counts.\n- `git diff origin/master --stat` touches exactly: pipeline/server_tools_lifecycle.py, pipeline/server.py, tests/unit/test_server_tools_lifecycle_cold_import.py.\n- Suggested commit: `fix(imports): register the lifecycle MCP tools from pipeline.server`.\n\nHARD CONSTRAINTS (read before touching anything)\n- Never run `git checkout <ref>`, `git switch --detach`, `git checkout --detach`, `git reset --hard` or `git clean` on this worktree. Stay on the branch you were given.\n- Do NOT call `restore_file`, `git checkout ... -- <path>` or `git restore` on anything. The harness blocks most of them, and `restore_file` restores from the LAST COMMIT, which is not the base branch. If an edit went wrong, stop and report it; do not try to undo it by hand.\n- Compare against `origin/master`, NEVER the bare `master` ref. The local `master` branch is deliberately stale, so `git diff master` lists files from stories merged since it last moved. Those are not yours: never revert, restore or edit a file that is not on your `files` list.\n- READ-TOOL CAP, NOT FILE CORRUPTION: a `view_file` result that ends in a truncation note means the read tool stopped, the file is intact. Re-read a narrower line range. Never conclude a file is damaged and never rewrite one to \"repair\" it.\n- Use `.venv/bin/python -m pytest` and `.venv/bin/python -m ruff`, never a bare `pytest` or `ruff`.\n- Run the full suite AT MOST TWICE; re-running a green suite burns your steps. When you have enough information, act.\n- `.agent_scratchpad.md` must NEVER be committed. If it shows up tracked, run `git rm --cached .agent_scratchpad.md`.\n- Do not add features, refactor adjacent code, or reformat lines you did not have to change.\n- Do NOT modify, rename or delete any existing test. No existing test needs to change for this story.\n- Do NOT use `str_replace` or `replace_lines` on any production file. The command under WHAT TO DO is the only way the production files change.\n- Commit with Conventional Commits, imperative subject <=72 chars, and a body that explains WHY.\n",
          "acceptance": [
            {
              "path": "tests/unit/test_server_tools_lifecycle_cold_import.py",
              "source": "\"\"\"pipeline.server_tools_lifecycle must import cold, and its tools must still register.\n\nThe module used to decorate its tools with ``@mcp.tool()`` where ``mcp`` was a lazy\nreference to pipeline.server's FastMCP instance. Decorating resolved the server at\nimport time, and pipeline.server imports this module, so a cold\n``import pipeline.server_tools_lifecycle`` re-entered it half-initialized and died\nwith an ImportError. The tools are now plain functions that pipeline.server registers.\ntests/unit/conftest.py pre-imports pipeline.server, which hides the failure\nin-process, so every probe here runs in a subprocess.\n\"\"\"\n\nimport subprocess\nimport sys\nfrom pathlib import Path\n\nimport pytest\n\nREPO_ROOT = Path(__file__).resolve().parents[2]\n\nTOOL_NAMES = [\n    \"request_decision\",\n    \"list_decisions\",\n    \"review_story\",\n    \"advance_pipeline\",\n    \"approve_merge\",\n    \"pause_plan\",\n    \"resume_plan\",\n    \"advance_all_plans\",\n]\n\n\ndef _probe(code):\n    return subprocess.run(\n        [sys.executable, \"-c\", code],\n        capture_output=True,\n        text=True,\n        check=False,\n        cwd=REPO_ROOT,\n    )\n\n\ndef test_server_tools_lifecycle_imports_cold_in_a_fresh_interpreter():\n    result = _probe(\"import pipeline.server_tools_lifecycle\")\n    assert result.returncode == 0, result.stderr\n\n\ndef test_importing_server_tools_lifecycle_does_not_load_the_server():\n    result = _probe(\"import sys, pipeline.server_tools_lifecycle; print('pipeline.server' in sys.modules)\")\n    assert result.returncode == 0, result.stderr\n    assert result.stdout.strip() == \"False\"\n\n\n@pytest.mark.parametrize(\"name\", TOOL_NAMES)\ndef test_server_registers_the_lifecycle_tool_and_re_exports_the_same_function(name):\n    result = _probe(\n        \"import pipeline.server as p, pipeline.server_tools_lifecycle as t; \"\n        f\"fn = getattr(p, {name!r}); \"\n        f\"print(fn is getattr(t, {name!r}), p.mcp._tool_manager._tools[{name!r}].fn is fn)\"\n    )\n    assert result.returncode == 0, result.stderr\n    assert result.stdout.strip() == \"True True\"\n\n\n@pytest.mark.parametrize(\"name\", TOOL_NAMES)\ndef test_lifecycle_first_then_server_registers_the_same_tool(name):\n    result = _probe(\n        \"import pipeline.server_tools_lifecycle as t, pipeline.server as p; \"\n        f\"print(p.mcp._tool_manager._tools[{name!r}].fn is getattr(t, {name!r}))\"\n    )\n    assert result.returncode == 0, result.stderr\n    assert result.stdout.strip() == \"True\"\n"
            }
          ],
          "persona": "software-engineer",
          "risk": "low",
          "backend": "ollama",
          "model": "glm-5.3-flash:cloud",
          "files": [
            "pipeline/server_tools_lifecycle.py",
            "pipeline/server.py"
          ]
        },
        {
          "key": "PCI2-3",
          "summary": "Bind story_status and ci onto the server from pipeline.server so both import cold",
          "description": "story_status and ci imported pipeline.server at module level to rebind functions onto its namespace, closing a cycle. Move each rebind/export block into a bind_to_server function that pipeline.server calls right after importing the module.",
          "agent_instructions": "Preflight: impact run 2026-09-24 on 931099f with PCI2-1 and PCI2-2 applied (scratch worktree, the real edit applied) - 13097 passed / 12 skipped / 0 failed (baseline 12976 plus PCI2-1's 4, PCI2-2's 18 and this fixture's 99); `ruff check .` clean under ruff 0.16.8 (CI's pinned version). The fixture was verified RED on the code with PCI2-1 and PCI2-2 applied (15 of its 99 tests fail on assertions carrying the ImportError text; the 84 that pass are the per-module sweep and the server-first controls; 0 import errors in the test file itself) and green with the real edit. 0 pre-existing tests conflict with the change: no existing test is edited.\n\nTARGET IMPLEMENTER: cloud open-source (backend \"ollama\", model \"glm-5.3-flash:cloud\"). Three production files (pipeline/story_status.py ~1002 lines, pipeline/ci.py ~882 lines, pipeline/server.py ~991 lines). The change wraps two existing import-time blocks in functions (about 75 re-indented lines in story_status, 25 in ci) and adds 3 short call sites in server.py. Two new functions (`bind_to_server` in each satellite), no documentation change. Two of the files are near or over the size cap and one block is re-indented, so the edit is scripted; the agent types nothing.\n\n[no-new-tests] The acceptance fixture named below is the complete, already-verified suite for this story. It is already in your worktree at the stated path. Do NOT write a second test suite, and do NOT modify, rename or delete that file. It is digest-pinned: any edit to it, even whitespace-only, marks it tampered and blocks the story. Never run `ruff format`, black or `ruff check --fix` over it or over any file. Use plain `.venv/bin/python -m ruff check .` to check hygiene. Commit the fixture together with your change (`git add` it) so it lands in the PR.\nFixture: tests/unit/test_hub_satellite_cold_imports.py (99 tests: one per pipeline module plus the binding checks). Every probe runs in a fresh subprocess because tests/unit/conftest.py pre-imports pipeline.server and would hide the defect in-process.\n\nWHY (the defect)\n`import pipeline.story_status` in a fresh interpreter dies with ImportError. story_status and ci each run `from pipeline import server as _server` at module level so they can rebind functions onto the server's namespace (`types.FunctionType(fn.__code__, _server.__dict__, ...)`), and export helper names into `_server.__dict__`. pipeline.server imports both, so a cold import of story_status reaches ci, which imports the server, which imports the half-initialized story_status. Fix: each satellite exposes `bind_to_server(_server)` holding that rebind/export block, and pipeline.server calls it immediately after importing the satellite. The timing is unchanged (same point in server's import), and the pytest-conditional exports keep their `\"pytest\" not in sys.modules` guard. Once every pipeline module imports cold, the fixture's per-module sweep guards it for good.\n\nWHAT TO DO (one step, one short command)\nThe whole change is already written as a script that lives OUTSIDE the repository, so there is nothing to type or copy. Run exactly this, from the repository root, in one bash call:\n\n.venv/bin/python /Users/jessecarroll/.claude/plans/pipeline-cold-imports-phase2.PCI2-3.apply.py\n\nIt (1) in pipeline/story_status.py removes the module-level `from pipeline import server as _server` and wraps the trailing rebind/export block in `def bind_to_server(_server)` (declaring `global check_story_status, _detached_grade_lifecycle`), (2) in pipeline/ci.py replaces the module-level `from . import server as _server` plus two `types.FunctionType` rebinds with `def bind_to_server(server)`, which also sets `server._mark_story_done_impl` and `server._record_retro_pending`, and (3) in pipeline/server.py adds `from . import ci as _ci_module` to the top import block, calls `_ci_module.bind_to_server(sys.modules[__name__])` right after the `from .ci import (...)` block, and replaces `from pipeline.story_status import check_story_status` with `from pipeline import story_status as _story_status`, `_story_status.bind_to_server(sys.modules[__name__])` and `check_story_status = _story_status.check_story_status`. The parameter in story_status is deliberately named `_server`: three existing tests pin the source text `_server.<name> = <name>`. It compiles every file and prints `OK 3` on success. It also checks each result against a hash of the verified reference and every assertion runs before any write, so it never leaves a half-edited file. Do NOT open, retype, paste or modify the script, and do NOT hand-edit any production file.\n\nIf it printed `OK 3`, go to VERIFY. If it raised an AssertionError or SyntaxError, do NOT improvise and do NOT hand-edit any file: run `git status --short` and `git diff origin/master --stat`, and report the exact message.\n\nVERIFY (once each)\n1. `git diff origin/master --stat` lists exactly pipeline/story_status.py, pipeline/ci.py, pipeline/server.py and tests/unit/test_hub_satellite_cold_imports.py.\n2. The tests named under SUCCESS CRITERIA are green.\n3. Commit everything in ONE step so the branch head carries the change: `git add pipeline/story_status.py pipeline/ci.py pipeline/server.py tests/unit/test_hub_satellite_cold_imports.py && git commit -m \"fix(imports): bind story_status and ci onto the server from pipeline.server\"`.\n\nWhat the fixture grades (implement exactly this): `story_status` and `ci` each import cold in a fresh interpreter without loading pipeline.server; every module under pipeline/ imports cold; after importing the server (first, or after the satellites) `check_story_status`, `_mark_story_done_impl` and `_record_retro_pending` are the satellites' own objects, their `__globals__` is the server's `__dict__`, and `check_story_status` is the function registered on the server's MCP instance; every helper story_status used to export onto the server (`_detached_grade_lifecycle`, `start_detached_grade`, `_untrack_scratchpad`, ...) is present on it outside pytest.\n\nSURVIVORS (do not change)\n- The bodies of `check_story_status`, `_detached_grade_lifecycle_unbound` and every other function in story_status.py and ci.py. Only the trailing rebind/export block of each file changes.\n- The two source-text pins: `_server.<name> = <name>` lines in story_status.py stay spelled exactly that way.\n- The lifecycle-tool registration block that PCI2-2 added to pipeline/server.py, and everything else in it.\n- pipeline/service.py (its `_ServerRef(\"_mark_story_done_impl\")` keeps resolving at call time) and every other module.\n- Every existing test passes UNMODIFIED.\n\nSUCCESS CRITERIA\n- `.venv/bin/python -m pytest -q tests/unit/test_hub_satellite_cold_imports.py tests/unit/test_story_status_activity_watchdog.py tests/unit/test_post_agent_scratchpad_untrack.py tests/unit/test_tick_grade_baseline_exemption.py tests/unit/test_check_story_status_lint_gate.py tests/unit/test_oa2_watchdog_streak.py` is green (186 tests).\n- Full suite green and `.venv/bin/python -m ruff check .` clean. Report the counts.\n- `git diff origin/master --stat` touches exactly: pipeline/story_status.py, pipeline/ci.py, pipeline/server.py, tests/unit/test_hub_satellite_cold_imports.py.\n- Suggested commit: `fix(imports): bind story_status and ci onto the server from pipeline.server`.\n\nHARD CONSTRAINTS (read before touching anything)\n- Never run `git checkout <ref>`, `git switch --detach`, `git checkout --detach`, `git reset --hard` or `git clean` on this worktree. Stay on the branch you were given.\n- Do NOT call `restore_file`, `git checkout ... -- <path>` or `git restore` on anything. The harness blocks most of them, and `restore_file` restores from the LAST COMMIT, which is not the base branch. If an edit went wrong, stop and report it; do not try to undo it by hand.\n- Compare against `origin/master`, NEVER the bare `master` ref. The local `master` branch is deliberately stale, so `git diff master` lists files from stories merged since it last moved. Those are not yours: never revert, restore or edit a file that is not on your `files` list.\n- READ-TOOL CAP, NOT FILE CORRUPTION: a `view_file` result that ends in a truncation note means the read tool stopped, the file is intact. Re-read a narrower line range. Never conclude a file is damaged and never rewrite one to \"repair\" it.\n- Use `.venv/bin/python -m pytest` and `.venv/bin/python -m ruff`, never a bare `pytest` or `ruff`.\n- Run the full suite AT MOST TWICE; re-running a green suite burns your steps. When you have enough information, act.\n- `.agent_scratchpad.md` must NEVER be committed. If it shows up tracked, run `git rm --cached .agent_scratchpad.md`.\n- Do not add features, refactor adjacent code, or reformat lines you did not have to change.\n- Do NOT modify, rename or delete any existing test. No existing test needs to change for this story.\n- Do NOT use `str_replace` or `replace_lines` on any production file. The command under WHAT TO DO is the only way the production files change.\n- Commit with Conventional Commits, imperative subject <=72 chars, and a body that explains WHY.\n",
          "acceptance": [
            {
              "path": "tests/unit/test_hub_satellite_cold_imports.py",
              "source": "\"\"\"pipeline.story_status and pipeline.ci must import cold, and still bind to the server.\n\nBoth modules used to run ``from pipeline import server as _server`` at module level\nso they could rebind functions onto the server's namespace (``types.FunctionType``\nwith ``_server.__dict__`` as the globals). pipeline.server imports both, so a cold\n``import pipeline.story_status`` re-entered a half-initialized module and died with an\nImportError. Each module now exposes ``bind_to_server(server)`` and pipeline.server calls\nit right after importing them. tests/unit/conftest.py pre-imports pipeline.server, which\nhides the failure in-process, so every probe here runs in a subprocess.\n\"\"\"\n\nimport subprocess\nimport sys\nfrom pathlib import Path\n\nimport pytest\n\nREPO_ROOT = Path(__file__).resolve().parents[2]\n\nSERVER_EXPORTS_FROM_STORY_STATUS = [\n    \"_detached_grade_lifecycle\",\n    \"DETACHED_GRADE_WATCHDOG_SECONDS\",\n    \"DISPATCH_STALE_ACTIVITY_SECONDS\",\n    \"collect_story_wedge_signals\",\n    \"_baseline_exempted_failures\",\n    \"_record_test_check\",\n    \"_clear_failure_streaks\",\n    \"start_detached_grade\",\n    \"collect_detached_grade\",\n    \"_untrack_scratchpad\",\n    \"_plan_conflict_intercept\",\n]\n\nALL_PIPELINE_MODULES = sorted(p.stem for p in (REPO_ROOT / \"pipeline\").glob(\"*.py\") if p.stem != \"__init__\")\n\n\ndef _probe(code):\n    return subprocess.run(\n        [sys.executable, \"-c\", code],\n        capture_output=True,\n        text=True,\n        check=False,\n        cwd=REPO_ROOT,\n    )\n\n\n@pytest.mark.parametrize(\"module\", [\"story_status\", \"ci\"])\ndef test_satellite_imports_cold_without_loading_the_server(module):\n    result = _probe(f\"import sys, pipeline.{module}; print('pipeline.server' in sys.modules)\")\n    assert result.returncode == 0, result.stderr\n    assert result.stdout.strip() == \"False\"\n\n\n@pytest.mark.parametrize(\"module\", ALL_PIPELINE_MODULES)\ndef test_every_pipeline_module_imports_cold_in_a_fresh_interpreter(module):\n    result = _probe(f\"import pipeline.{module}\")\n    assert result.returncode == 0, result.stderr\n\n\n_BOUND_TO_SERVER = (\n    \"fn = p.check_story_status; \"\n    \"print(fn is s.check_story_status, fn.__globals__ is p.__dict__, \"\n    \"p.mcp._tool_manager._tools['check_story_status'].fn is fn); \"\n    \"print(p._mark_story_done_impl is c._mark_story_done_impl, p._mark_story_done_impl.__globals__ is p.__dict__); \"\n    \"print(p._record_retro_pending is c._record_retro_pending, p._record_retro_pending.__globals__ is p.__dict__)\"\n)\n_EXPECTED_BOUND = \"True True True\\nTrue True\\nTrue True\"\n\n\ndef test_server_first_binds_the_satellite_functions_onto_the_server():\n    result = _probe(\"import pipeline.server as p, pipeline.story_status as s, pipeline.ci as c; \" + _BOUND_TO_SERVER)\n    assert result.returncode == 0, result.stderr\n    assert result.stdout.strip() == _EXPECTED_BOUND\n\n\ndef test_satellites_first_then_server_binds_the_satellite_functions_onto_the_server():\n    result = _probe(\"import pipeline.story_status as s, pipeline.ci as c, pipeline.server as p; \" + _BOUND_TO_SERVER)\n    assert result.returncode == 0, result.stderr\n    assert result.stdout.strip() == _EXPECTED_BOUND\n\n\n@pytest.mark.parametrize(\"name\", SERVER_EXPORTS_FROM_STORY_STATUS)\ndef test_story_status_exports_its_collaborators_onto_the_server_namespace(name):\n    result = _probe(f\"import pipeline.story_status, pipeline.server as p; print(hasattr(p, {name!r}))\")\n    assert result.returncode == 0, result.stderr\n    assert result.stdout.strip() == \"True\"\n"
            }
          ],
          "persona": "software-engineer",
          "risk": "low",
          "backend": "ollama",
          "model": "glm-5.3-flash:cloud",
          "files": [
            "pipeline/story_status.py",
            "pipeline/ci.py",
            "pipeline/server.py"
          ],
          "dependencies": [
            "PCI2-1",
            "PCI2-2"
          ]
        }
      ]
    }
  ]
}
